# 09-04 Summary

Implemented the top-level Portfolio UI.

- Added `/portfolio` route.
- Added `ExposureMatrix` and `CorrelatedRiskSection`.
- Added portfolio query hooks and TypeScript types.
- Added the Portfolio nav link.

Verification:

- `cd frontend && npx tsc --noEmit`
- `cd frontend && npm run build`
