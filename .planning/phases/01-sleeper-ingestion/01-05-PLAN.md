---
phase: 01-sleeper-ingestion
plan: "05"
type: execute
wave: 5
depends_on:
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - backend/src/fantasy/ingestion/gap_detector.py
  - backend/src/fantasy/routers/health.py
  - backend/src/fantasy/main.py
  - backend/tests/test_gap_detector.py
autonomous: false
requirements:
  - INGEST-08

user_setup:
  - service: fantasyPros-or-adp-source
    why: "Global Asset Baseline seeding requires a dynasty ADP source. FantasyPros dynasty ADP is available as a free CSV download. The executor will implement the loader; the user must supply the CSV file at data/adp_baseline.csv."
    dashboard_config:
      - task: "Download dynasty ADP CSV"
        location: "FantasyPros dynasty rankings page -> Export CSV button. Save as data/adp_baseline.csv in the project root."

must_haves:
  truths:
    - "Data freshness status is queryable per league showing last_run_at, status, and all gaps from the most recent ingest run"
    - "A gap with a missing Sleeper scoring key is surfaced with name, reason, and expected resolution phase"
    - "A gap with a missing nfl_data_py season (e.g., 2025 data not available) is surfaced explicitly"
    - "GET /health/{league_id} returns HTTP 200 with gap list (empty list if no gaps)"
    - "The ADP baseline CSV is loaded into a player_adp_baseline table at startup if the file exists at data/adp_baseline.csv"
    - "User can inspect the app in a browser at http://localhost:8000/docs and see all routes documented"
  artifacts:
    - path: "backend/src/fantasy/ingestion/gap_detector.py"
      provides: "GapDetector: identifies and labels data gaps from ingest run results"
      exports: ["GapDetector", "DataGap"]
    - path: "backend/src/fantasy/routers/health.py"
      provides: "FastAPI router: GET /health/{league_id}"
      exports: ["router"]
  key_links:
    - from: "IngestService.run"
      to: "GapDetector.collect_all"
      via: "called during ingest after apply_corrections; gaps_json written to ingest_runs table"
      pattern: "GapDetector.collect_all"
    - from: "GET /health/{league_id}"
      to: "ingest_runs table"
      via: "queries most recent complete run's gaps_json"
      pattern: "gaps_json"

---

> Execution status note (2026-03-12): See `.planning/phases/01-sleeper-ingestion/01-ENGINEER-HANDOFF.md` for completed vs deferred implementation details.


<objective>
Build the gap detection system, health endpoint, and global asset baseline seeder. This is the final implementation plan for Phase 1 ??? after this, the full ingest pipeline is operational and the system surfaces data quality information explicitly.

Purpose: INGEST-08 requires that data gaps are never silently dropped. The gap detector enforces this contract by naming, explaining, and attributing each gap to an expected resolution phase. The ADP baseline loader seeds the Global Asset Baseline that Phase 2's valuation engine builds on.

Output: `gap_detector.py`; health router at GET /health/{league_id}; ADP baseline loader; phase verification checkpoint.
</objective>

<execution_context>
@C:/Users/gspea/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gspea/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-sleeper-ingestion/01-CONTEXT.md
@.planning/phases/01-sleeper-ingestion/01-RESEARCH.md
@.planning/phases/01-sleeper-ingestion/01-00-SUMMARY.md
@.planning/phases/01-sleeper-ingestion/01-03-SUMMARY.md
@.planning/phases/01-sleeper-ingestion/01-04-SUMMARY.md

<interfaces>
<!-- Gap format from CONTEXT.md locked decisions -->
Gap display format: "[Gap name]: [Why it's missing]. Expected resolution: Phase X."
Example: "2025 Season Stats: nfl_data_py does not yet contain 2025 weekly data. Expected resolution: Phase 1 re-ingest after nflverse update."
Example: "Scoring Key 'def_td': No nfl_data_py column mapped for defensive touchdowns. Expected resolution: Phase 1 ??? add to SLEEPER_TO_NFLDATA_MAP."

<!-- GapDetector contract: takes ingest run results, produces list of DataGap -->
Gaps are written to ingest_runs.gaps_json as a JSON array.

<!-- ingest_service.py state after Plan 04 -->
IngestService.run() in ingest_service.py already calls OverrideService.apply_corrections() at end of
successful run (added in Plan 04). This plan appends GapDetector.collect_all() AFTER that call.
Do NOT remove or reorder the apply_corrections call ??? append only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GapDetector, health router, and ADP baseline loader</name>
  <files>
    backend/src/fantasy/ingestion/gap_detector.py,
    backend/src/fantasy/routers/health.py,
    backend/tests/test_gap_detector.py
  </files>
  <behavior>
    - DataGap Pydantic model: name (str), reason (str), expected_resolution (str), severity ("warning" | "info")
    - DataGap.format() -> str: returns "[name]: [reason]. Expected resolution: [expected_resolution]." per CONTEXT.md format
    - GapDetector.detect_unmapped_scoring_keys(scoring_settings: dict, sleeper_to_nfldata_map: dict) -> list[DataGap]: returns one DataGap per key in scoring_settings not present in the map
    - GapDetector.detect_missing_season_stats(df: polars.DataFrame, expected_years: list[int]) -> list[DataGap]: returns DataGap for each year where df has 0 rows
    - GapDetector.detect_incomplete_transaction_history(conn, league_id: str, expected_season_year: int) -> list[DataGap]: queries ingest_runs.cursor_json to check if all weeks were fetched
    - test_gap_surfacing: call detect_unmapped_scoring_keys with scoring_settings containing an unmapped key ("def_td"); assert one DataGap returned with name containing "def_td"
    - test_gap_surfacing_missing_year: call detect_missing_season_stats with empty DataFrame for year 2025; assert DataGap returned mentioning "2025"
    - GapDetector.collect_all(conn, league_id, scoring_settings, stats_df, expected_years) -> list[DataGap]: runs all detectors and returns combined list
    - Health router: GET /health/{league_id} queries most recent ingest_run for the league; returns {league_id, last_run_at, status, gap_count, gaps: [DataGap.model_dump()]} ??? empty gaps list if no run yet
    - Health router returns HTTP 200 always (not 404/503) ??? gaps are in the response body, not HTTP status
  </behavior>
  <action>
Create `backend/src/fantasy/ingestion/gap_detector.py`.

`DataGap`:
```python
class DataGap(BaseModel):
    name: str
    reason: str
    expected_resolution: str
    severity: Literal["warning", "info"] = "warning"

    def format(self) -> str:
        return f"{self.name}: {self.reason}. Expected resolution: {self.expected_resolution}."
```

`class GapDetector` with three static detector methods + one `collect_all` orchestrator.

For `detect_unmapped_scoring_keys`: iterate `scoring_settings.keys()`; for each key not in `sleeper_to_nfldata_map`, create DataGap with:
- name = f"Unmapped Scoring Key: {key}"
- reason = f"No nfl_data_py column mapped for Sleeper scoring key '{key}'."
- expected_resolution = "Phase 1 ??? add to SLEEPER_TO_NFLDATA_MAP in nfl_data_loader.py."

For `detect_missing_season_stats`: check `df.filter(pl.col("season") == year).height == 0` for each expected year; create DataGap per missing year.

Update `IngestService.run()` (in ingest_service.py, which already has the apply_corrections call from Plan 04): APPEND gap detection after the existing apply_corrections call ??? do NOT remove apply_corrections. Call `GapDetector.collect_all(...)` and write `gaps_json = json.dumps([g.model_dump() for g in gaps])` to the ingest_run row.

Create `backend/src/fantasy/routers/health.py`:
```python
router = APIRouter(prefix="/health", tags=["health"])

@router.get("/{league_id}")
async def get_health(league_id: str) -> dict:
    # Query: SELECT * FROM ingest_runs WHERE league_id = ? ORDER BY started_at DESC LIMIT 1
    # Parse gaps_json, return structured response
```

Replace stubs in `backend/tests/test_gap_detector.py`:
- test_gap_surfacing: assert detect_unmapped_scoring_keys returns gap for "def_td" when "def_td" not in SLEEPER_TO_NFLDATA_MAP
- test_gap_surfacing_missing_year: build empty polars DataFrame with schema matching player_stats_weekly, call detect_missing_season_stats with [2025], assert DataGap with "2025" in name

Wire health router into `backend/src/fantasy/main.py`: `app.include_router(health.router)`.

**ADP Baseline Loader:** Also create a function in `nfl_data_loader.py` (already exists from Plan 02):
```python
def load_adp_baseline(conn: duckdb.DuckDBPyConnection, csv_path: str = "data/adp_baseline.csv") -> int:
    """Load dynasty ADP CSV into player_adp_baseline table. Returns rows loaded. No-ops if file does not exist."""
```
Add `player_adp_baseline` table to `db/models.py` and a new Alembic migration `003_add_adp_baseline.py`. Table columns: player_id VARCHAR, player_name VARCHAR, position VARCHAR, adp FLOAT, adp_source VARCHAR, loaded_at TIMESTAMP. No PRIMARY KEY constraint ??? ADP data is replaced wholesale on each load.

Wire `load_adp_baseline` into FastAPI app startup lifespan: if `data/adp_baseline.csv` exists, load it at startup. If file does not exist, log "ADP baseline file not found at data/adp_baseline.csv ??? skipping. See user_setup in 01-05-PLAN.md."
  </action>
  <verify>
    <automated>cd C:/Users/gspea/Fantasy/backend && python -m pytest tests/test_gap_detector.py -v 2>&1</automated>
  </verify>
  <done>test_gap_surfacing passes for unmapped key; test_gap_surfacing_missing_year passes for empty 2025 data; health router registered in main.py; ADP baseline loader no-ops gracefully when CSV missing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 1 Pipeline: End-to-End Human Verification</name>
  <files>none</files>
  <action>Human verification ??? no automated action required; all implementation completed in prior tasks. Follow the how-to-verify steps below.</action>
  <verify>
    <automated>cd C:/Users/gspea/Fantasy/backend && python -m pytest tests/ -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>User confirms all 7 verification steps pass and types "approved".</done>
  <what-built>
    Complete Phase 1 ingest pipeline:
    - Project scaffold with DuckDB schema and Alembic migrations (Plan 00)
    - SleeperMapper domain model adapter (Plan 01)
    - LeagueRepo DuckDB persistence + nfl_data_py loader with fantasy point reconstruction (Plan 02)
    - SleeperClient async HTTP client + IngestService orchestration with all-weeks transaction loop (Plan 03)
    - Manual corrections system (OverrideService) + FastAPI routers for ingest and corrections (Plan 04)
    - Gap detector + health endpoint + ADP baseline loader (Plan 05)
  </what-built>
  <how-to-verify>
    Step 1 ??? Start the API server:
    ```
    cd backend
    pip install -e ".[dev]"
    alembic upgrade head
    uvicorn fantasy.main:app --reload
    ```
    Expected: Server starts at http://localhost:8000 with no errors.

    Step 2 ??? Check API docs:
    Visit http://localhost:8000/docs
    Expected: Swagger UI shows /ingest, /corrections, /health route groups.

    Step 3 ??? Trigger an ingest (requires a real Sleeper league ID you own):
    ```
    curl -X POST "http://localhost:8000/ingest/{YOUR_LEAGUE_ID}"
    ```
    Expected: JSON response with run_id and status="complete" (or "failed" with an error_message if API is unreachable).

    Step 4 ??? Check health endpoint:
    ```
    curl "http://localhost:8000/health/{YOUR_LEAGUE_ID}"
    ```
    Expected: JSON with last_run_at, status, and gaps list. Any gaps should include name, reason, and expected_resolution fields in the "[Gap name]: [reason]. Expected resolution: Phase X." format.

    Step 5 ??? Create a manual correction:
    ```
    curl -X POST "http://localhost:8000/corrections/" \
      -H "Content-Type: application/json" \
      -d '{"league_id": "{YOUR_LEAGUE_ID}", "entity_type": "player", "entity_id": "4017", "field": "position", "corrected_value": "WR", "original_value": "RB"}'
    ```
    Expected: 201 response with correction id.

    Step 6 ??? Verify correction survives re-ingest:
    Run ingest again: `curl -X POST "http://localhost:8000/ingest/{YOUR_LEAGUE_ID}"`
    Then: `curl "http://localhost:8000/corrections/{YOUR_LEAGUE_ID}"`
    Expected: Correction from Step 5 still present.

    Step 7 ??? Run full test suite:
    ```
    cd backend && pytest tests/ -v --tb=short
    ```
    Expected: All tests pass (stubs that haven't been replaced will still show FAILED ??? that is acceptable for Wave 0 stubs marked with "STUB:" in their fail message, but no ERROR or unexpected failures).
  </how-to-verify>
  <resume-signal>Type "approved" when all steps pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 gate ??? all must be true before `/gsd:verify-work`:
1. `pytest tests/ -v --tb=short` ??? no ERROR results (FAILED stubs are acceptable if their fail message starts with "STUB:")
2. `uvicorn fantasy.main:app` starts without ImportError or RuntimeError
3. `/docs` shows ingest, corrections, health route groups
4. Manual correction created before re-ingest persists after re-ingest
5. Health endpoint returns gap list in "[Gap name]: [reason]. Expected resolution: Phase X." format
</verification>

<success_criteria>
- GET /health/{league_id} returns structured gap list with name/reason/expected_resolution per gap
- GapDetector.collect_all returns DataGap objects (not strings, not None)
- ADP baseline loader no-ops gracefully when data/adp_baseline.csv does not exist
- Full pytest suite: 0 ERROR, all non-stub tests green
- Human-verify checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/01-sleeper-ingestion/01-05-SUMMARY.md`
</output>
