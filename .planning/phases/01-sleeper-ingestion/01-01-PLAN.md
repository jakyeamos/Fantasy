---
phase: 01-sleeper-ingestion
plan: "01"
type: execute
wave: 2
depends_on:
  - "01-00"
files_modified:
  - backend/src/fantasy/ingestion/sleeper_mapper.py
  - backend/tests/test_sleeper_mapper.py
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-04

must_haves:
  truths:
    - "A raw Sleeper league JSON dict is converted to a LeagueSettings domain model with all format flags correctly derived"
    - "superflex is True when SUPER_FLEX appears in roster_positions; False otherwise"
    - "tep is True when scoring_settings has bonus_rec_te > 0; False otherwise"
    - "ppr is the float value of scoring_settings['rec'] (0, 0.5, or 1.0)"
    - "A raw Sleeper roster JSON dict is converted to a RosterSnapshot model with starters, bench, IR, and taxi arrays correctly separated"
    - "A raw traded_picks response list is converted to a list of TradedPick models"
    - "A Sleeper response missing optional fields (None values) does not raise a validation error"
  artifacts:
    - path: "backend/src/fantasy/ingestion/sleeper_mapper.py"
      provides: "SleeperMapper class + all domain model Pydantic types"
      exports: ["LeagueSettings", "RosterSnapshot", "TradedPick", "StandingRow", "TransactionRecord", "SleeperMapper"]
    - path: "backend/tests/test_sleeper_mapper.py"
      provides: "Full test suite replacing stubs from 01-00"
      contains: "test_map_league_settings"
  key_links:
    - from: "backend/src/fantasy/ingestion/sleeper_mapper.py"
      to: "pydantic.BaseModel"
      via: "LeagueSettings inherits"
      pattern: "class LeagueSettings.*BaseModel"
    - from: "SleeperMapper.map_league"
      to: "LeagueSettings"
      via: "derives superflex/tep/ppr flags inline"
      pattern: "SUPER_FLEX.*roster_positions"
---

<objective>
Build the SleeperMapper: the adapter-first isolation layer that converts raw Sleeper API JSON dicts into typed Pydantic v2 domain models. All downstream layers (repositories, services, routers) consume only these domain models — never raw dicts.

Purpose: This is the adapter boundary locked in STATE.md. Getting this right prevents Sleeper API changes from propagating through the system. Every subsequent plan depends on these types as contracts.

Output: `sleeper_mapper.py` with all Phase 1 domain models and `SleeperMapper` static methods. Full tests replacing the stubs from Plan 00.
</objective>

<execution_context>
@C:/Users/gspea/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gspea/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-sleeper-ingestion/01-CONTEXT.md
@.planning/phases/01-sleeper-ingestion/01-RESEARCH.md
@.planning/phases/01-sleeper-ingestion/01-00-SUMMARY.md

<interfaces>
<!-- Domain model contracts this plan creates. Downstream plans depend on these types. -->

From RESEARCH.md Pattern 1 (adapter-first isolation):
The mapper must produce these types. All other layers import ONLY from sleeper_mapper.py for domain models.

Key raw Sleeper field shapes (from RESEARCH.md):
- League: {"league_id", "name", "season", "scoring_settings": {str: float}, "roster_positions": [str], "settings": {}}
- Roster: {"roster_id": int, "owner_id": str|None, "league_id": str, "starters": [str], "players": [str], "reserve": [str]|None, "taxi": [str]|None}
- TradedPick: {"season": str, "round": int, "roster_id": int, "owner_id": int, "previous_owner_id": int|None, "league_id": str}
- Transaction: {"transaction_id": str, "type": str, "status": str, "created": int (ms epoch), "roster_ids": [int], "adds": {str: int}|None, "drops": {str: int}|None, "draft_picks": [...], "leg": int}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain models and SleeperMapper static methods</name>
  <files>backend/src/fantasy/ingestion/sleeper_mapper.py</files>
  <behavior>
    - LeagueSettings: league_id, name, season are required str fields; scoring_settings is dict[str,float]; roster_positions is list[str]; settings_blob is dict; superflex/tep are bool; ppr is float
    - superflex=True when "SUPER_FLEX" in roster_positions
    - tep=True when scoring_settings.get("bonus_rec_te", 0.0) > 0
    - ppr=scoring_settings.get("rec", 0.0)
    - RosterSnapshot: starters, bench, ir, taxi are all list[str]; bench = set(players) - set(starters) - set(reserve or []) - set(taxi or [])
    - TradedPick: season, round, roster_id, owner_id required; previous_owner_id is Optional[int]
    - StandingRow: league_id, roster_id required; wins/losses/ties/fpts/fpts_against from roster["settings"]
    - TransactionRecord: created_at is datetime (converted from ms epoch integer); adds/drops/draft_picks default to empty if None in raw
    - SleeperMapper.map_league(raw: dict) -> LeagueSettings raises ValidationError for missing required fields
    - SleeperMapper.map_roster(raw: dict) -> RosterSnapshot handles None for reserve and taxi arrays
    - SleeperMapper.map_traded_picks(raw: list[dict]) -> list[TradedPick]
    - SleeperMapper.map_standing(raw: dict, league_id: str) -> StandingRow reads from raw["settings"] subdict
    - SleeperMapper.map_transactions(raw: list[dict]) -> list[TransactionRecord] converts ms epoch to datetime
    - Partial response: map_roster with reserve=None returns empty ir list, not crash
    - Partial response: map_league with missing scoring_settings key defaults gracefully (ppr=0.0, tep=False)
  </behavior>
  <action>
Create `backend/src/fantasy/ingestion/sleeper_mapper.py`.

Define all Pydantic v2 domain models at the top of the file (these are the exported contracts):

```python
from pydantic import BaseModel, field_validator
from datetime import datetime
from typing import Optional

class LeagueSettings(BaseModel): ...
class RosterSnapshot(BaseModel): ...
class TradedPick(BaseModel): ...
class StandingRow(BaseModel): ...
class TransactionRecord(BaseModel): ...
```

Then define `class SleeperMapper` with static methods.

Important implementation notes:
- Use Pydantic v2 `model_validate()` not `parse_obj()` (v1 API — deprecated)
- Use `model_dump()` not `.dict()` (v1 API — deprecated)
- For `bench` computation in RosterSnapshot: bench = all_players minus starters, IR, taxi
- For datetime conversion in TransactionRecord: `datetime.fromtimestamp(created_ms / 1000)` — Sleeper timestamps are milliseconds since epoch
- For scoring_settings.get() calls: always provide 0.0 default — missing keys must not crash

Do NOT import from `fantasy.db.models` — sleeper_mapper.py is a pure-Python adapter with no DB dependency.

Export at module level: `__all__ = ["LeagueSettings", "RosterSnapshot", "TradedPick", "StandingRow", "TransactionRecord", "SleeperMapper"]`
  </action>
  <verify>
    <automated>cd C:/Users/gspea/Fantasy/backend && python -m pytest tests/test_sleeper_mapper.py -x -q 2>&1</automated>
  </verify>
  <done>All test_sleeper_mapper.py tests pass green; SleeperMapper importable; all 5 domain model types exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete test_sleeper_mapper.py (replace stubs)</name>
  <files>backend/tests/test_sleeper_mapper.py</files>
  <behavior>
    - test_map_league_settings: maps mock_league_response; asserts league_id="test_league_001", season="2025", ppr=1.0, superflex=True, tep=True
    - test_flag_derivation: test without SUPER_FLEX in roster_positions -> superflex=False; test bonus_rec_te=0 -> tep=False; test rec=0.5 -> ppr=0.5
    - test_map_roster: maps mock_roster_response; asserts starters=["4017","4663"], taxi=["8888"], ir=["5122"], "2374" in bench, "6945" in bench
    - test_map_traded_picks: maps mock_traded_pick_response (2 items); asserts first pick season="2025" round=1; second pick previous_owner_id is None
    - test_partial_response_handling: map_roster with reserve=None returns ir=[]; map_league with empty scoring_settings returns ppr=0.0, tep=False, superflex=False
  </behavior>
  <action>
Replace the `pytest.fail("STUB: ...")` bodies in `backend/tests/test_sleeper_mapper.py` with real assertions against `SleeperMapper`.

Import `SleeperMapper` and all domain models from `fantasy.ingestion.sleeper_mapper`.

Each test function receives fixtures from conftest.py by name — ensure fixture parameter names match exactly (`mock_league_response`, `mock_roster_response`, `mock_traded_pick_response`).

For `test_flag_derivation`, build small inline dicts rather than using fixtures — keeps tests readable and independent of fixture details.

For `test_partial_response_handling`, construct minimal dicts with only required fields and verify no exception is raised (use `assert result.ir == []` and similar).

Do NOT add `import pytest` at the top if it is already imported. The tests must be deterministic — no side effects, no network calls, no file I/O.
  </action>
  <verify>
    <automated>cd C:/Users/gspea/Fantasy/backend && python -m pytest tests/test_sleeper_mapper.py -v 2>&1</automated>
  </verify>
  <done>All 5 test functions in test_sleeper_mapper.py pass green; no FAILED or ERROR results; 0 warnings about async_mode.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pytest tests/test_sleeper_mapper.py -v` — all tests green
2. `python -c "from fantasy.ingestion.sleeper_mapper import SleeperMapper, LeagueSettings, RosterSnapshot, TradedPick, StandingRow, TransactionRecord; print('all imports OK')"` — no ImportError
3. `pytest tests/ -x -q -k "not integration"` — no regressions from Plan 00 stubs
</verification>

<success_criteria>
- All 5 SleeperMapper tests pass (test_map_league_settings, test_flag_derivation, test_map_roster, test_map_traded_picks, test_partial_response_handling)
- LeagueSettings, RosterSnapshot, TradedPick, StandingRow, TransactionRecord all exported from sleeper_mapper.py
- SleeperMapper has 4 static mapping methods: map_league, map_roster, map_traded_picks, map_standing, map_transactions
- No DB imports in sleeper_mapper.py (pure adapter layer)
</success_criteria>

<output>
After completion, create `.planning/phases/01-sleeper-ingestion/01-01-SUMMARY.md`
</output>
