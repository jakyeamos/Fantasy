---
phase: 02-team-intelligence
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/004_phase2_output_tables.py
  - backend/src/fantasy/intelligence/__init__.py
  - backend/src/fantasy/intelligence/constants.py
  - backend/src/fantasy/intelligence/models.py
  - backend/tests/test_scorecard_engine.py
  - backend/tests/test_direction_engine.py
  - backend/tests/test_valuation_engine.py
  - backend/tests/test_intelligence_service.py
  - backend/tests/test_intelligence_router.py
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-03
  - TEAM-04
  - TEAM-05
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04

must_haves:
  truths:
    - "Alembic migration 004 creates team_scorecards, team_directions, and player_values tables without error against an in-memory DuckDB"
    - "fantasy.intelligence package is importable; constants.py exports DIRECTION_WEIGHTS, DIRECTION_MOVE_MATRIX, FORMAT_MULTIPLIERS, DIRECTION_VALUE_WEIGHTS, POSITIONAL_PEAK_AGE, POSITIONAL_CLIFF_AGE, ROUND_WEIGHTS, MOVE_TYPE_TARGETS"
    - "models.py exports TeamScorecard, DirectionResult, PlayerValue Pydantic v2 models with all required fields"
    - "All 5 test stub files are discoverable by pytest and contain FAILING placeholder tests — not empty files"
    - "pytest backend/tests/ -x -q exits non-zero (stubs fail) but collects without import errors"
  artifacts:
    - path: "backend/alembic/versions/004_phase2_output_tables.py"
      provides: "DDL for team_scorecards, team_directions, player_values"
      contains: "team_scorecards"
    - path: "backend/src/fantasy/intelligence/constants.py"
      provides: "All weight vectors, age thresholds, move matrix constants"
      exports: ["DIRECTION_WEIGHTS", "DIRECTION_MOVE_MATRIX", "POSITIONAL_PEAK_AGE", "FORMAT_MULTIPLIERS"]
    - path: "backend/src/fantasy/intelligence/models.py"
      provides: "Pydantic domain models for Phase 2 engine outputs"
      exports: ["TeamScorecard", "DirectionResult", "PlayerValue"]
    - path: "backend/tests/test_scorecard_engine.py"
      provides: "Failing stubs for all TEAM-01 unit tests"
      contains: "test_all_subscores_present"
    - path: "backend/tests/test_direction_engine.py"
      provides: "Failing stubs for TEAM-02 through TEAM-05 unit tests"
      contains: "test_valid_label"
    - path: "backend/tests/test_valuation_engine.py"
      provides: "Failing stubs for PLAY-01 through PLAY-04 unit tests"
      contains: "test_all_components_present"
  key_links:
    - from: "backend/src/fantasy/intelligence/models.py"
      to: "pydantic.BaseModel"
      via: "TeamScorecard, DirectionResult, PlayerValue inherit"
      pattern: "class TeamScorecard.*BaseModel"
    - from: "backend/alembic/versions/004_phase2_output_tables.py"
      to: "team_scorecards table"
      via: "op.execute() DDL"
      pattern: "CREATE TABLE.*team_scorecards"
---

<objective>
Create the Wave 0 foundation for Phase 2: Alembic migration for output tables, the intelligence package skeleton with all constants and domain models, and failing test stubs for all five test files.

Purpose: Every subsequent plan in Phase 2 depends on (a) the output tables existing, (b) the Pydantic models being importable, (c) the constants being defined, and (d) the test stubs being in place so engine plans can replace them with real tests as they go. This plan must complete before Plans 01, 02, and 03 can begin.

Output: Importable `fantasy.intelligence` package with constants and models, Alembic migration 004, and 5 failing test stub files.
</objective>

<execution_context>
@C:/Users/gspea/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gspea/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-team-intelligence/02-RESEARCH.md

<interfaces>
Phase 1 tables this phase reads from (already exist):
- `leagues` — league_id, scoring_settings, roster_positions, settings (superflex, tep, ppr flags)
- `rosters` — roster_id, league_id, starters (JSON string), players (JSON string), reserve, taxi
- `players` — player_id, position, age, full_name
- `player_stats_weekly` — player_id, season, week, fantasy_points, games_played + stat columns
- `traded_picks` — league_id, season, round, roster_id (original owner), owner_id (current holder)
- `player_adp_baseline` — player_id, adp_rank, position
- `corrections` — entity_type, entity_id, field, corrected_value (user overrides — must be applied before scoring)
- `transactions` — transaction_id, league_id, type, roster_ids, adds, drops, draft_picks, created

Phase 2 output tables this migration creates (see RESEARCH.md Architecture Patterns section for full DDL):
- `team_scorecards` — 9 sub-scores + composite per team per league
- `team_directions` — direction label, confidence, reasoning, alternates_json, delta_json, move lists
- `player_values` — 12 component scores + 5 lens aggregates per player per league per team

Constants required (from RESEARCH.md Pattern 2, Pattern 4, Pattern 5):
- DIRECTION_WEIGHTS: 8 direction labels × sub-score weight dict (including all 8: true_contender, fragile_contender, fringe_playoff, productive_struggle, one_year_punt, retool, elite_value_accumulation, hard_rebuild)
- DIRECTION_MOVE_MATRIX: approved/discouraged move lists per direction label
- POSITIONAL_PEAK_AGE and POSITIONAL_CLIFF_AGE: {"RB": 26, "WR": 28, "TE": 30, "QB": 32} / {"RB": 29, "WR": 31, "TE": 33, "QB": 35}
- ROUND_WEIGHTS: {1: 3.0, 2: 2.0, 3: 1.0, 4: 0.5}
- FORMAT_MULTIPLIERS: PPR tiers + superflex + TEP multipliers per position
- DIRECTION_VALUE_WEIGHTS: 8 direction labels × 12 component weight dicts
- MOVE_TYPE_TARGETS: move_type → sub-score it addresses

Domain models required (from RESEARCH.md Architecture Patterns):
- TeamScorecard: league_id, roster_id, computed_at, win_now, future_value, depth, pick_capital, flexibility, fragility, age_risk, liquidity, positional_insulation, composite; method as_dict() → dict[str, float]
- DirectionResult: primary_label, confidence, reasoning, alternates (list of dicts), delta (dict), approved_moves, discouraged_moves
- PlayerValue: league_id, roster_id, player_id, computed_at, comp_* (12 fields), lens_* (5 fields)
- ScorecardInputs: internal model for passing gathered data into sub-score methods
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration 004 + intelligence package skeleton</name>
  <files>
    backend/alembic/versions/004_phase2_output_tables.py
    backend/src/fantasy/intelligence/__init__.py
  </files>
  <action>
    Create `backend/alembic/versions/004_phase2_output_tables.py` with revision ID `004`, down_revision pointing to the existing `003` migration. Use `op.execute()` for DuckDB-compatible DDL (Alembic's `op.create_table` generates ANSI DDL that does not support DuckDB-specific syntax). The DDL must exactly match the schema in RESEARCH.md Architecture Patterns section (team_scorecards, team_directions, player_values tables with all columns listed there). Include `IF NOT EXISTS` guards. The downgrade() method must drop all three tables.

    Verify the migration chain by checking that `003` already exists (from Phase 1's `003_add_adp_baseline.py`) and that the revision ID `004` does not collide with any existing file in `backend/alembic/versions/`.

    Create `backend/src/fantasy/intelligence/__init__.py` as an empty package marker (one line: `# Phase 2 intelligence engines`).

    Do NOT add scikit-learn, numpy, or pandas to pyproject.toml — they are not needed per RESEARCH.md.
  </action>
  <verify>
    cd backend && python -m alembic upgrade head
    Expect: migration 004 applies without error.
    Also: python -c "from fantasy.intelligence import __init__" exits 0.
  </verify>
  <done>Migration file exists, applies cleanly, creates all three output tables. Package __init__.py is importable.</done>
</task>

<task type="auto">
  <name>Task 2: Constants and domain models</name>
  <files>
    backend/src/fantasy/intelligence/constants.py
    backend/src/fantasy/intelligence/models.py
  </files>
  <action>
    Create `backend/src/fantasy/intelligence/constants.py` with ALL eight direction labels fully defined. Do not leave any direction as a comment placeholder — all 8 must have complete weight dicts for DIRECTION_WEIGHTS and DIRECTION_MOVE_MATRIX. The four documented in RESEARCH.md (true_contender, fragile_contender, hard_rebuild, and partial patterns for others) must be extrapolated to cover: fringe_playoff, productive_struggle, one_year_punt, retool, elite_value_accumulation. Use domain judgment consistent with the research: fringe_playoff prioritizes win_now moderately + depth, productive_struggle balances win_now + future_value, one_year_punt maximizes pick_capital + youth, retool balances age_risk reduction + maintaining some win_now, elite_value_accumulation prioritizes liquidity + future_value + pick_capital.

    Also define: POSITIONAL_PEAK_AGE, POSITIONAL_CLIFF_AGE, ROUND_WEIGHTS (exact values from RESEARCH.md), FORMAT_MULTIPLIERS (all PPR tiers + superflex + TEP), DIRECTION_VALUE_WEIGHTS (12 component weights per all 8 directions), MOVE_TYPE_TARGETS. Each constant must have a source comment (e.g., `# Source: RESEARCH.md Pattern 5 / ESPN age curve analysis 2023`).

    Create `backend/src/fantasy/intelligence/models.py` with three Pydantic v2 BaseModel classes:
    - `TeamScorecard`: all fields from the DDL schema plus a `def as_dict(self) -> dict[str, float]` method returning only the 9 sub-score float fields (not id, league_id, roster_id, computed_at, composite, computation_json)
    - `DirectionResult`: primary_label (str), confidence (float), reasoning (str), alternates (list[dict]), delta (dict), approved_moves (list[str]), discouraged_moves (list[str])
    - `PlayerValue`: all fields from the DDL schema (league_id, roster_id, player_id, computed_at, 12 comp_* floats, 5 lens_* floats). All component/lens fields are `float | None` with default None (missing stats are valid).
    - `ScorecardInputs`: internal model (not persisted) holding the gathered raw data for one team: starters, bench, ir, taxi (list[str] each), player_ages (dict[str, int]), player_positions (dict[str, str]), weekly_fantasy_pts (dict[str, float]), adp_ranks (dict[str, float]), pick_rows (list[tuple]), correction_overrides (dict[str, Any])

    Use `model_config = ConfigDict(frozen=False)` for all models (mutable during computation).
  </action>
  <verify>
    python -c "from fantasy.intelligence.constants import DIRECTION_WEIGHTS, DIRECTION_MOVE_MATRIX, POSITIONAL_PEAK_AGE, FORMAT_MULTIPLIERS; assert len(DIRECTION_WEIGHTS) == 8; assert len(DIRECTION_MOVE_MATRIX) == 8; print('constants OK')"
    python -c "from fantasy.intelligence.models import TeamScorecard, DirectionResult, PlayerValue; sc = TeamScorecard(league_id='x', roster_id=1, win_now=0.5, future_value=0.5, depth=0.5, pick_capital=0.5, flexibility=0.5, fragility=0.5, age_risk=0.5, liquidity=0.5, positional_insulation=0.5, composite=0.5); print(sc.as_dict()); print('models OK')"
  </verify>
  <done>All 8 direction labels present in both DIRECTION_WEIGHTS and DIRECTION_MOVE_MATRIX. All three domain models importable and constructable. TeamScorecard.as_dict() returns a dict with exactly 9 keys.</done>
</task>

<task type="auto">
  <name>Task 3: Failing test stubs for all Phase 2 test files</name>
  <files>
    backend/tests/test_scorecard_engine.py
    backend/tests/test_direction_engine.py
    backend/tests/test_valuation_engine.py
    backend/tests/test_intelligence_service.py
    backend/tests/test_intelligence_router.py
  </files>
  <action>
    Create five test stub files. Each stub must:
    1. Import the relevant module path (even though the engine files don't exist yet — the import will fail, causing the test to fail with ImportError, which counts as a failing test)
    2. Contain at least the test function names listed in RESEARCH.md Validation Architecture section
    3. Have a `pytest.mark.xfail(reason="stub — engine not yet implemented")` decorator on each test OR raise `NotImplementedError` in the body — use the `raises NotImplementedError` pattern (simpler)

    `test_scorecard_engine.py`: stub tests for — test_all_subscores_present, test_subscores_in_range, test_pick_capital_dual_source, test_missing_stats_defaults, test_corrections_applied_before_scoring
    Import target: `from fantasy.intelligence.scorecard_engine import ScorecardEngine`

    `test_direction_engine.py`: stub tests for — test_valid_label, test_contender_signal, test_rebuild_signal, test_confidence_range, test_minimum_alternates, test_delta_present, test_move_matrix_completeness, test_ranked_moves
    Import target: `from fantasy.intelligence.direction_engine import DirectionEngine`

    `test_valuation_engine.py`: stub tests for — test_all_components_present, test_missing_stats_defaults, test_ppr_format_adjustment, test_superflex_adjustment, test_direction_reweight, test_five_lenses
    Import target: `from fantasy.intelligence.valuation_engine import ValuationEngine`

    `test_intelligence_service.py`: stub tests for — test_idempotent_compute, test_scorecard_triggers_direction_recompute
    Import target: `from fantasy.intelligence.intelligence_service import IntelligenceService`

    `test_intelligence_router.py`: stub tests for — test_scorecard_endpoint, test_direction_endpoint, test_player_value_endpoint
    Import target: `from fantasy.intelligence.intelligence_service import IntelligenceService` (router wiring tested in Plan 04)

    Use `pytest.importorskip` pattern only if needed. Prefer letting the import fail naturally — pytest will report them as errors (not passes), which satisfies "failing, not empty."

    Do NOT create the engine files themselves. The stubs are deliberately incomplete.
  </action>
  <verify>
    cd backend && pytest tests/test_scorecard_engine.py tests/test_direction_engine.py tests/test_valuation_engine.py tests/test_intelligence_service.py tests/test_intelligence_router.py -q 2>&1 | tail -5
    Expect: all tests fail/error (ImportError on engine files), zero passes, no collection errors on the stub files themselves.
    Also: pytest backend/tests/ -q --collect-only 2>&1 | grep "test_scorecard\|test_direction\|test_valuation\|test_intelligence" | wc -l should show ≥ 20 test items collected.
  </verify>
  <done>All 5 stub files exist. pytest collects ≥ 20 test items across them. Every collected item fails (import error or NotImplementedError). No test passes.</done>
</task>

</tasks>

<verification>
Run after all tasks complete:
- `cd backend && python -m alembic upgrade head` — exits 0
- `python -c "from fantasy.intelligence.constants import DIRECTION_WEIGHTS; assert len(DIRECTION_WEIGHTS) == 8"` — exits 0
- `python -c "from fantasy.intelligence.models import TeamScorecard, DirectionResult, PlayerValue"` — exits 0
- `pytest backend/tests/test_scorecard_engine.py backend/tests/test_direction_engine.py backend/tests/test_valuation_engine.py -q 2>&1 | grep -E "passed|error|failed"` — expect 0 passed, all error or failed
</verification>

<success_criteria>
- Migration 004 exists and applies cleanly to in-memory DuckDB creating all 3 output tables
- `fantasy.intelligence` package is importable with constants.py and models.py fully populated
- All 8 direction labels present in DIRECTION_WEIGHTS and DIRECTION_MOVE_MATRIX with non-empty move lists
- 5 test stub files exist, all discoverable by pytest, all failing (not empty)
- `pytest backend/tests/ -q --collect-only` shows ≥ 20 new test items from Phase 2 stubs
</success_criteria>

<output>
After completion, create `.planning/phases/02-team-intelligence/02-00-SUMMARY.md`
</output>
