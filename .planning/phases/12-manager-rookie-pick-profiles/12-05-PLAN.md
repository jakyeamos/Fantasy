---
phase: 12-manager-rookie-pick-profiles
plan: 05
type: execute
wave: 3
depends_on:
  - 12-02
  - 12-03
files_modified:
  - frontend/src/components/ManagerListRow.tsx
  - frontend/src/routes/draft-room.tsx
autonomous: false
requirements:
  - FS-06

must_haves:
  truths:
    - "ManagerListRow shows 'Picks Buyer' badge when manager has non-null pick_premium_score >= 0.10 in the list response"
    - "Badge is fully absent (not muted, not disabled) when pick_premium_score is null or below threshold (D-04, D-17)"
    - "Draft room view shows manager tendency warnings alongside existing positional run and value gap warnings (D-05)"
    - "Manager tendency warnings use same TendencyWarningList render path — no new layout (D-06)"
    - "User can confirm the full end-to-end flow works: dossier shows 4th tab (if evidence present), overview card visible, badges appear in manager list"
  artifacts:
    - path: "frontend/src/components/ManagerListRow.tsx"
      provides: "Picks Buyer badge conditional on pick_premium_score from ManagerSummary"
    - path: "frontend/src/routes/draft-room.tsx"
      provides: "Manager tendency warnings rendered via existing TendencyWarningList"
  key_links:
    - from: "frontend/src/components/ManagerListRow.tsx"
      to: "ManagerSummary.pick_premium_score"
      via: "Badge rendered when pick_premium_score is non-null in list endpoint response"
      pattern: "Picks Buyer|pick_premium_score"
    - from: "frontend/src/routes/draft-room.tsx"
      to: "TendencyWarningList"
      via: "draft room API response now includes manager_tendency warnings; same component renders them"
      pattern: "manager_tendency|TendencyWarningList"
---

<objective>
Wire the Phase 12 signals into the two remaining frontend surfaces: the manager list row "Picks Buyer" badge and the draft room manager tendency warnings. Then run a human-verify checkpoint to confirm the full end-to-end flow.

Purpose: This is the final wiring plan — all backend signals are live, the dossier is built, and these two surfaces complete the integration contract for FS-06.
Output: ManagerListRow badge + draft room warning extension + human verification
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-manager-rookie-pick-profiles/12-CONTEXT.md
@.planning/phases/12-manager-rookie-pick-profiles/12-UI-SPEC.md
@.planning/phases/12-manager-rookie-pick-profiles/12-02-SUMMARY.md
@.planning/phases/12-manager-rookie-pick-profiles/12-03-SUMMARY.md
@.planning/phases/12-manager-rookie-pick-profiles/12-04-SUMMARY.md

<interfaces>
<!--
Key types and contracts. Extracted from codebase.

=== ManagerSummary list endpoint response (after plan 02 extension) ===

Fields (all required unless noted):
  league_id           string
  roster_id           number
  manager_name        string
  direction_label     string | null
  exploitability_score  number
  evidence_count      number
  low_confidence      boolean
  top_pitch_angle     PitchAngle | null
  pick_premium_score  number | null  -- null = no evidence or below threshold (pre-filtered by backend per D-17)

=== TendencyWarning shape (after plan 03 extension) ===

Fields:
  warning_type    "positional_run" | "value_gap" | "manager_tendency"
  title           string  -- e.g. "Manager X targets WR early"
  description     string
  affected_players  string[]

=== ManagerListRow existing patterns ===

Renders one manager row in the list.
Already shows exploitability score, low_confidence flag, top_pitch_angle.
Badge import already present from existing usage.
Row layout: flex items-center gap-2 for inline badge placement.

=== TendencyWarningList existing patterns ===

Renders warnings list from TendencyWarning array.
Row pattern: rounded-xl border border-border/45 bg-card/45 p-4 per row
warning.title: text-sm font-semibold
warning.description: text-sm text-muted-foreground
No structural change needed — just passes more items in the array.

=== UI-SPEC badge spec ===

"Picks Buyer" badge uses variant="default" (cyan --primary), inline with existing exploitability block.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Picks Buyer badge to ManagerListRow + update frontend ManagerSummary type</name>
  <read_first>
    - frontend/src/components/ManagerListRow.tsx (full file — current props interface, Badge usage, flex row layout for badge placement)
    - frontend/src/routes/league.$leagueId.managers.tsx (full file — how ManagerListRow is called; where ManagerSummary type is imported or defined)
  </read_first>
  <files>
    frontend/src/components/ManagerListRow.tsx
  </files>
  <action>
Read `frontend/src/components/ManagerListRow.tsx` fully before editing.

1. Find the `ManagerSummary` TypeScript type used by `ManagerListRow` (either in the component itself or imported from a types file). Add `pick_premium_score?: number | null` to that type.

2. In the component's props interface, ensure `pick_premium_score` flows through (it likely comes from the ManagerSummary data already — confirm the prop shape matches the API response structure).

3. In the JSX, inside the existing `flex items-center gap-2` row that shows the exploitability score and top pitch angle, add:
```tsx
{/* Picks Buyer badge — absent when null (D-04, D-17 silent omission) */}
{manager.pick_premium_score !== null && manager.pick_premium_score !== undefined && (
  <Badge variant="default">Picks Buyer</Badge>
)}
```

The badge must:
- Use `variant="default"` (cyan `--primary`) per UI-SPEC color discipline section
- Label exactly `Picks Buyer` (per copywriting contract)
- Be fully absent (not rendered at all) when `pick_premium_score` is null or undefined
- Be positioned inline, not on a new line — inside the existing flex row with exploitability

The backend already filters: `pick_premium_score` in the list response is non-null only when score >= PICK_PREMIUM_THRESHOLD. The frontend does not need to re-apply the threshold check — null = absent, non-null = show.

No other changes to this file.
  </action>
  <verify>
    <automated>cd /Users/jakyeamos/Desktop/Fantasy/frontend && npx tsc --noEmit 2>&1 | grep "ManagerListRow" | head -10; echo "check done"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ManagerListRow.tsx` contains `Picks Buyer` text in JSX
    - `frontend/src/components/ManagerListRow.tsx` contains `variant="default"` on the Picks Buyer badge
    - `frontend/src/components/ManagerListRow.tsx` badge is wrapped in a null-check conditional (`pick_premium_score !== null`)
    - TypeScript check shows no errors for ManagerListRow
    - The badge label is exactly `Picks Buyer` (not "Pick Buyer", not "Picks buyer")
  </acceptance_criteria>
  <done>ManagerListRow shows Picks Buyer badge inline with exploitability when pick_premium_score is non-null from API response</done>
</task>

<task type="auto">
  <name>Task 2: Wire manager tendency warnings in draft room frontend</name>
  <read_first>
    - frontend/src/routes/draft-room.tsx (full file — how TendencyWarningList is rendered; where tendency_warnings come from; TendencyWarning type definition or import)
    - frontend/src/components/draft-room/TendencyWarningList.tsx (full file — props interface; how warning_type is used if at all)
  </read_first>
  <files>
    frontend/src/routes/draft-room.tsx
  </files>
  <action>
Read `frontend/src/routes/draft-room.tsx` and `frontend/src/components/draft-room/TendencyWarningList.tsx` fully before editing.

The backend (plan 03) already adds `manager_tendency` warning items to the draft room API response. The frontend needs no structural changes — only ensure the `TendencyWarning` TypeScript type includes `"manager_tendency"` in the `warning_type` union.

Find the `TendencyWarning` or `WarningType` TypeScript type in the frontend codebase (likely in `draft-room.tsx` or a shared types file). Update it:
```typescript
// Before:
type WarningType = "positional_run" | "value_gap"
// After:
type WarningType = "positional_run" | "value_gap" | "manager_tendency"
```

Or if it is an interface field:
```typescript
warning_type: "positional_run" | "value_gap" | "manager_tendency"
```

Verify that `TendencyWarningList` renders `warning.title` and `warning.description` verbatim (from UI-SPEC). If it filters on `warning_type` to display different UI, confirm that `"manager_tendency"` warnings display correctly using the same render path (D-06 — same treatment, no new layout).

No changes to `TendencyWarningList.tsx` are expected — the backend provides the correct title/description text. The only change needed is the TypeScript type union extension to prevent a type error.
  </action>
  <verify>
    <automated>cd /Users/jakyeamos/Desktop/Fantasy/frontend && npx tsc --noEmit 2>&1 | grep -E "draft-room|TendencyWarning" | head -10; echo "tsc done"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/routes/draft-room.tsx` or the shared types file contains `"manager_tendency"` in the `warning_type` union
    - TypeScript check shows no errors for draft-room route
    - `TendencyWarningList.tsx` is NOT structurally changed (D-06 — same render path)
  </acceptance_criteria>
  <done>Draft room frontend accepts manager_tendency warning_type without TypeScript error; manager tendency warnings render via existing TendencyWarningList</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Phase 12 end-to-end smoke test</name>
  <what-built>
    Complete Phase 12 implementation:
    - Backend: RookiePickProfileEngine computing pick-premium score, positional tendency, dominant archetype
    - Backend: ManagerProfile API response extended with show_draft_picks_tab and all rookie/pick fields
    - Backend: POST /leagues/{league_id}/ingest/draft-picks on-demand endpoint
    - Backend: picks_buyer reroute type in trade evaluator
    - Backend: manager_tendency warnings in draft room
    - Frontend: RookiePickMarketCard in Overview tab
    - Frontend: DossierDraftPicksTab (conditionally shown via show_draft_picks_tab)
    - Frontend: Picks Buyer badge in ManagerListRow
    - Frontend: manager_tendency warnings in draft room
  </what-built>
  <how-to-verify>
    1. Start both backend and frontend: `cd backend && uvicorn fantasy.main:app --reload` + `cd frontend && npm run dev`

    2. Navigate to any league's managers list. Verify:
       - Manager rows render without errors
       - No "Picks Buyer" badge visible (expected — no draft pick data ingested yet; pick_premium_score will be null for all managers)

    3. Navigate to any manager dossier. Verify:
       - Overview tab renders with "Rookie & Pick Market" card visible
       - "Rookie & Pick Market" card shows positional tendency row, dominant archetype row
       - No pick-premium badge in the card (expected — no pick-trade evidence yet)
       - No "Draft & Picks" tab visible in tab bar (expected — show_draft_picks_tab = false)
       - No JavaScript errors in browser console

    4. Navigate to draft room for any league. Verify:
       - Draft room loads without errors
       - No manager tendency warnings visible (expected — no draft evidence ingested yet)
       - Existing positional run and value gap warnings still display correctly

    5. Call the on-demand draft pick ingest endpoint:
       POST http://localhost:8000/profiling/leagues/{your_league_id}/ingest/draft-picks
       Verify it returns JSON like: {"ingested": N, "league_id": "..."}
       (N may be 0 if no drafts exist for the league yet — this is fine for the smoke test)

    6. Open the trade evaluator, propose a trade where you would receive players (no picks). Verify no JavaScript errors.

    Report: "approved" if all surfaces load without errors and the expected empty states are correct, or describe specific errors.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all surfaces load correctly, or describe any errors seen</resume-signal>
</task>

</tasks>

<verification>
```
cd /Users/jakyeamos/Desktop/Fantasy/frontend && npx tsc --noEmit 2>&1 | tail -10
cd /Users/jakyeamos/Desktop/Fantasy/backend && python -m pytest tests/ -x -q 2>&1 | tail -10
```
All TypeScript types valid. All backend tests pass.
</verification>

<success_criteria>
- Picks Buyer badge renders in ManagerListRow for managers with non-null pick_premium_score (D-04)
- Badge is fully absent when pick_premium_score is null — not muted, not disabled (D-17)
- Draft room frontend accepts "manager_tendency" warning_type without TypeScript error (D-06)
- Manager tendency warnings display via existing TendencyWarningList with no structural changes
- Human verification confirms all surfaces load without errors and empty states are correct
</success_criteria>

<output>
After completion, create `.planning/phases/12-manager-rookie-pick-profiles/12-05-SUMMARY.md`
</output>
