---
phase: 06-dynamic-pick-valuation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/fantasy/picks/__init__.py
  - backend/src/fantasy/picks/constants.py
  - backend/src/fantasy/picks/models.py
  - backend/src/fantasy/picks/pick_repo.py
  - backend/alembic/versions/007_pick_values.py
  - backend/tests/picks/__init__.py
  - backend/tests/picks/test_pick_engine.py
  - backend/tests/picks/test_pick_repo.py
  - backend/tests/integration/test_pick_router.py
autonomous: true
requirements: [PICK-01, PICK-02]

must_haves:
  truths:
    - "All pick valuation constants are named in constants.py with no magic numbers"
    - "Pydantic models define the full pick valuation contract including Phase 7 class_strength hook"
    - "PickRepo reads standings, picks, manager profiles, and rebuilder count from DuckDB"
    - "pick_values table exists via Alembic migration with all required columns"
  artifacts:
    - path: "backend/src/fantasy/picks/constants.py"
      provides: "All Phase 6 named constants — slot decay, calendar multipliers, demand weights, timing thresholds"
      contains: "CALENDAR_TIMING_MULTIPLIERS"
    - path: "backend/src/fantasy/picks/models.py"
      provides: "PickValue, PickValuationContext, LeaguePickContext, TimingLabel, TeamStandingsRow"
      exports: ["PickValue", "PickValuationContext", "LeaguePickContext", "TimingLabel"]
    - path: "backend/src/fantasy/picks/pick_repo.py"
      provides: "DuckDB reads from picks, standings, team_directions, trades tables"
      exports: ["PickRepo"]
    - path: "backend/alembic/versions/007_pick_values.py"
      provides: "pick_values table DDL with unique constraint"
      contains: "pick_values"
    - path: "backend/tests/picks/test_pick_engine.py"
      provides: "Test stub for pick engine unit tests"
      contains: "test_"
  key_links:
    - from: "backend/src/fantasy/picks/pick_repo.py"
      to: "standings table"
      via: "DuckDB SELECT with last-N-game trend window"
      pattern: "standings"
    - from: "backend/src/fantasy/picks/pick_repo.py"
      to: "team_directions table"
      via: "DuckDB SELECT for rebuilder count"
      pattern: "team_directions"
    - from: "backend/src/fantasy/picks/pick_repo.py"
      to: "trades table"
      via: "DuckDB SELECT for pick reception rate"
      pattern: "pick_reception_rate"
---

<objective>
Build the Phase 6 backend foundation: all pick valuation constants, Pydantic domain models (including the Phase 7 class_strength hook), the PickRepo data access layer, the pick_values Alembic migration, and test stubs for downstream plans.

Purpose: Establish the full contract (types, constants, data access) that the PickEngine and router will build against in Plans 02 and 03.
Output: `backend/src/fantasy/picks/` package with constants, models, repo; migration 007; test stubs.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dynamic-pick-valuation/06-CONTEXT.md
@.planning/phases/06-dynamic-pick-valuation/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from prior phases. -->

From backend/src/fantasy/trade/models.py (Phase 5):
```python
class TradedPick(BaseModel):
    owner_roster_id: int
    original_roster_id: int
    year: int
    round: int
    league_id: str
```

From backend/src/fantasy/intelligence/models.py (Phase 2):
```python
class DirectionResult(BaseModel):
    primary_label: str  # one of 8 direction labels
    confidence: float
    reasoning: str
```

From backend/src/fantasy/profiling/models.py (Phase 4):
```python
class ManagerProfile(BaseModel):
    roster_id: int
    league_id: str
    primary_label: str
    # ... trade history patterns
```

Established patterns:
- DuckDB connection via `backend/src/fantasy/routers/deps.py` dependency injection
- Package layout: `__init__.py`, `constants.py`, `models.py`, `{name}_repo.py`, `{name}_engine.py`
- Router registered in `main.py` via `app.include_router()`
- Alembic migrations in `backend/alembic/versions/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create picks package with constants and models</name>
  <files>backend/src/fantasy/picks/__init__.py, backend/src/fantasy/picks/constants.py, backend/src/fantasy/picks/models.py</files>
  <action>
Create the `backend/src/fantasy/picks/` package directory.

**__init__.py:** Empty init, just marks the package.

**constants.py:** Copy ALL named constants from 06-RESEARCH.md "Pattern 4: Constants File Structure" section verbatim. This includes:
- SLOT_VALUE_DECAY_EXPONENT (0.6), FIRST_ROUND_BASE_VALUE (100.0)
- CALENDAR_TIMING_MULTIPLIERS dict (month -> float, all 12 months)
- NEAR_PEAK_MONTHS frozenset({2, 3, 4})
- CLASS_STRENGTH_WEIGHT (0.20) — per D-01/D-02, the weight stays at 0.20 but signal is 0.0
- NEUTRAL_REBUILDER_RATIO (0.25), MAX_REBUILDER_PREMIUM (0.15)
- DIRECTION_DEMAND_WEIGHT (0.6), HISTORY_DEMAND_WEIGHT (0.4)
- DEMAND_HISTORY_SIGMOID_K (8.0), DEMAND_HISTORY_SIGMOID_CENTER (0.40)
- DEMAND_FACTOR_MAX_PREMIUM (0.10)
- MIN_TRADE_EVIDENCE_THRESHOLD (10) — same as Phase 4, per D-09
- EARLY_PICK_SLOT_THRESHOLD (4), TRENDING_WINDOW (4), TRENDING_DOWN_THRESHOLD (0.25)
- SEASON_MIDPOINT_THRESHOLD (5)
- FUTURE_YEAR_DISCOUNT_RATE (0.80) — for multi-year picks (Pitfall 2)
- TOTAL_SEASON_GAMES (14) — standard dynasty season length
- DIRECTION_DEMAND_MAP: dict mapping all 8 direction labels to demand scores (hard_rebuild=1.0 through true_contender=0.1) per research
- TIMING_REASONING_TEMPLATES: dict with 4 template strings per research

All constants MUST be typed with explicit type annotations. No magic numbers anywhere else in the codebase.

**models.py:** Create Pydantic models per 06-RESEARCH.md:
- `TimingLabel(str, Enum)`: SELL_NOW, HOLD_UNTIL_ROOKIE_FEVER, USE_ON_THE_CLOCK
- `TeamStandingsRow(BaseModel)`: roster_id, wins, losses, win_pct, remaining_games, recent_wins, total_games, draft_in_progress (bool, default False)
- `LeaguePickContext(BaseModel)`: league_id, league_size, rebuilder_count, rebuilder_ratio, total_teams
- `PickValuationContext(BaseModel)`: pick (TradedPick), league_id, class_strength_signal (float, Field default=0.0, ge=-1.0, le=1.0, with Phase 7 description per research), target_manager_demand_factor (float, default=0.5)
- `PickValue(BaseModel)`: pick (TradedPick), base_value, timed_value, league_adjusted_value, demand_adjusted_value, expected_draft_slot, timing_label (TimingLabel), timing_reasoning (str), class_strength_signal (float), computed_at (datetime)

Import TradedPick from `fantasy.trade.models`. All float fields should have reasonable ge/le bounds where applicable.
  </action>
  <verify>
    <automated>cd /Users/jakyeamos/Desktop/Fantasy/backend && python -c "from fantasy.picks.constants import CALENDAR_TIMING_MULTIPLIERS, DIRECTION_DEMAND_MAP; from fantasy.picks.models import PickValue, PickValuationContext, TimingLabel; print('OK')"</automated>
  </verify>
  <done>All constants are named and typed in constants.py, all Pydantic models importable with Phase 7 hook field present on PickValuationContext</done>
</task>

<task type="auto">
  <name>Task 2: Create PickRepo, Alembic migration, and test stubs</name>
  <files>backend/src/fantasy/picks/pick_repo.py, backend/alembic/versions/007_pick_values.py, backend/tests/picks/__init__.py, backend/tests/picks/test_pick_engine.py, backend/tests/picks/test_pick_repo.py, backend/tests/integration/test_pick_router.py</files>
  <action>
**pick_repo.py:** Create `PickRepo` class following the same pattern as `trade_repo.py` (accepts DuckDB connection in __init__).

Methods:
1. `get_standings(roster_id: int, league_id: str) -> TeamStandingsRow` — DuckDB query from standings table with last-N-game trend window per research SQL example. Guard for remaining_games=0 (Pitfall 1). Return TeamStandingsRow.
2. `get_league_pick_context(league_id: str) -> LeaguePickContext` — DuckDB query from team_directions table to count rebuilders (labels in hard_rebuild, elite_value_accumulation, one_year_punt) per research SQL example. Computes rebuilder_ratio. Falls back to neutral ratio if no team_directions data exists (Pitfall 3).
3. `get_manager_demand_factor(roster_id: int, league_id: str) -> float` — Two queries: (a) direction label from team_directions -> direction demand score via DIRECTION_DEMAND_MAP, (b) pick reception rate from trades table per research SQL example. Combines using DIRECTION_DEMAND_WEIGHT/HISTORY_DEMAND_WEIGHT. Suppresses history signal below MIN_TRADE_EVIDENCE_THRESHOLD per D-09. Falls back to 0.5 (neutral) if no data exists (Pitfall 3).
4. `get_all_picks(league_id: str) -> list[TradedPick]` — Reads picks table for all future picks in the league.
5. `save_pick_values(values: list[PickValue]) -> None` — Upserts into pick_values table using UNIQUE constraint on (league_id, pick_owner_roster_id, pick_year, pick_round).

All methods must handle missing data gracefully — return sensible defaults, never raise KeyError for absent manager profiles.

**007_pick_values.py:** Alembic migration creating the pick_values table per research DDL. Include all columns: id (INTEGER PRIMARY KEY), league_id, pick_owner_roster_id, pick_year, pick_round, computed_at, expected_draft_slot, base_value, timed_value, league_adjusted_value, demand_adjusted_value, timing_label, timing_reasoning, class_strength_signal (DEFAULT 0.0), computation_json. UNIQUE constraint on (league_id, pick_owner_roster_id, pick_year, pick_round).

**Test stubs:** Create `backend/tests/picks/__init__.py` (empty), `test_pick_engine.py` (stub with `# Plan 02 populates this` comment and one placeholder `test_placeholder` that passes), `test_pick_repo.py` (stub with `test_placeholder`), and `backend/tests/integration/test_pick_router.py` (stub with `test_placeholder`).
  </action>
  <verify>
    <automated>cd /Users/jakyeamos/Desktop/Fantasy/backend && python -c "from fantasy.picks.pick_repo import PickRepo; print('OK')" && python -m pytest tests/picks/ -x --no-header -q</automated>
  </verify>
  <done>PickRepo importable with all 5 methods defined, migration 007 exists with pick_values DDL, all test stubs pass</done>
</task>

</tasks>

<verification>
- `from fantasy.picks.constants import *` imports all named constants without error
- `from fantasy.picks.models import PickValue, PickValuationContext, LeaguePickContext, TimingLabel, TeamStandingsRow` succeeds
- `PickValuationContext(pick=..., league_id="x").class_strength_signal == 0.0` (Phase 7 hook defaults to neutral)
- `from fantasy.picks.pick_repo import PickRepo` succeeds
- `backend/alembic/versions/007_pick_values.py` exists with `pick_values` CREATE TABLE
- All test stubs in `tests/picks/` pass
</verification>

<success_criteria>
The picks package is complete with constants, models, repo, and migration. Plan 02 can build the PickEngine against these contracts without exploring the codebase for types or table schemas.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dynamic-pick-valuation/06-01-SUMMARY.md`
</output>
