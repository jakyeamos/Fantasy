---
phase: 06-dynamic-pick-valuation
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - frontend/src/components/picks/TimingBadge.tsx
  - frontend/src/components/picks/PickValueSummaryRow.tsx
  - frontend/src/components/picks/LeaguePickList.tsx
  - frontend/src/components/AssetChip.tsx
  - frontend/src/components/EvaluationOutputPanel.tsx
  - frontend/src/lib/api/picks.ts
  - frontend/src/types/picks.ts
autonomous: false
requirements: [PICK-01, PICK-02, PICK-03]

must_haves:
  truths:
    - "Timing badges appear inline on every pick display surface per D-07 — sell now (amber), hold (blue), on the clock (green)"
    - "Each timing rec shows one-line reasoning below the badge per D-06"
    - "Demand-adjusted value appears in trade evaluator pick context per D-04"
    - "League pick list shows dynamic values with batch fetch per UI-SPEC"
    - "Phase 5 AssetChip pick variant extended without breaking player chip variant"
  artifacts:
    - path: "frontend/src/components/picks/TimingBadge.tsx"
      provides: "Timing badge component with three-state color treatment"
      exports: ["TimingBadge"]
    - path: "frontend/src/components/picks/LeaguePickList.tsx"
      provides: "League-level pick list with dynamic values and recompute button"
      exports: ["LeaguePickList"]
    - path: "frontend/src/lib/api/picks.ts"
      provides: "TanStack Query hooks for pick value API"
      exports: ["useLeaguePickValues", "useRecomputePicks"]
    - path: "frontend/src/types/picks.ts"
      provides: "TypeScript types matching backend PickValue response"
      exports: ["PickValue", "TimingLabel"]
  key_links:
    - from: "frontend/src/lib/api/picks.ts"
      to: "GET /picks/{league_id}"
      via: "fetch call"
      pattern: "/picks/"
    - from: "frontend/src/components/AssetChip.tsx"
      to: "frontend/src/components/picks/TimingBadge.tsx"
      via: "component import"
      pattern: "TimingBadge"
    - from: "frontend/src/components/picks/LeaguePickList.tsx"
      to: "frontend/src/lib/api/picks.ts"
      via: "useLeaguePickValues hook"
      pattern: "useLeaguePickValues"
---

<objective>
Extend all existing pick display surfaces with dynamic values and timing recommendations: AssetChip pick variant, trade evaluator pick rows, and league drill-in pick capital section.

Purpose: Make Phase 6 engine output visible wherever picks appear, per D-03/D-04 (no new pages, pure in-place upgrade). End with human verification.
Output: Updated pick UI across 3 surfaces + human-verify checkpoint.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dynamic-pick-valuation/06-CONTEXT.md
@.planning/phases/06-dynamic-pick-valuation/06-UI-SPEC.md
@.planning/phases/06-dynamic-pick-valuation/06-03-SUMMARY.md

<interfaces>
<!-- Backend API contract from Plan 03 -->

GET /picks/{league_id} returns:
```typescript
interface PickValue {
  pick: {
    owner_roster_id: number;
    original_roster_id: number;
    year: number;
    round: number;
    league_id: string;
  };
  base_value: number;
  timed_value: number;
  league_adjusted_value: number;
  demand_adjusted_value: number;
  expected_draft_slot: number;
  timing_label: "sell_now" | "hold_until_rookie_fever" | "use_on_the_clock";
  timing_reasoning: string;
  class_strength_signal: number;
  computed_at: string; // ISO datetime
}
```

<!-- Phase 5 existing components to extend -->

From frontend/src/components/AssetChip.tsx (Phase 5):
- Pick chip variant renders: [pick identity text] [projected slot badge]
- Currently `inline-flex items-center gap-2`
- Phase 6 changes pick variant to `flex flex-col gap-1` to add timing row
- Player chip variant MUST remain unchanged (single-row inline-flex)

From frontend/src/components/EvaluationOutputPanel.tsx (Phase 5):
- Pick rows within trade evaluator party columns
- Phase 6 adds demand-adjusted value + timing display per UI-SPEC PickValueSummaryRow

From Phase 3 league drill-in view:
- Pick capital section shows picks in a static list
- Phase 6 replaces with dynamic LeaguePickList component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript types, API hooks, and TimingBadge component</name>
  <files>frontend/src/types/picks.ts, frontend/src/lib/api/picks.ts, frontend/src/components/picks/TimingBadge.tsx</files>
  <action>
**types/picks.ts:** Define TypeScript types matching the backend PickValue response:
- `TimingLabel` type: `"sell_now" | "hold_until_rookie_fever" | "use_on_the_clock"`
- `PickValue` interface with all fields from the API contract above
- `TimingBadgeProps`: { label: TimingLabel; reasoning: string; showReasoning?: boolean }

**lib/api/picks.ts:** TanStack Query hooks:
- `useLeaguePickValues(leagueId: string)` — `useQuery` calling `GET /picks/{leagueId}`, returns `PickValue[]`. `staleTime: 5 * 60 * 1000` (5 minutes per UI-SPEC note 5). `enabled: !!leagueId`.
- `useRecomputePicks(leagueId: string)` — `useMutation` calling `POST /picks/{leagueId}/recompute`. Invalidates the `useLeaguePickValues` query on success.

**components/picks/TimingBadge.tsx:** Stateless component rendering the timing badge per UI-SPEC.

Three-state color classes (from UI-SPEC Color section):
- `sell_now`: `bg-amber-100 dark:bg-amber-950/30 text-amber-800 dark:text-amber-200`
- `hold_until_rookie_fever`: `bg-blue-50 dark:bg-blue-950/20 text-blue-700 dark:text-blue-300`
- `use_on_the_clock`: `bg-green-50 dark:bg-green-950/20 text-green-700 dark:text-green-300`

Badge: `rounded px-2 py-1 text-xs font-medium` + state color classes. NOT a shadcn Badge (per UI-SPEC note 4).

Display labels per Copywriting Contract: "Sell now", "Hold", "On the clock".

If `showReasoning` is true (default true), render timing reasoning string on a second line: `mt-1 text-xs text-muted-foreground`. Reasoning is always `text-muted-foreground` regardless of timing state.
  </action>
  <verify>
    <automated>cd /Users/jakyeamos/Desktop/Fantasy/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript types compile, TanStack Query hooks defined, TimingBadge renders three states with correct colors</done>
</task>

<task type="auto">
  <name>Task 2: Extend AssetChip, create PickValueSummaryRow, and LeaguePickList</name>
  <files>frontend/src/components/AssetChip.tsx, frontend/src/components/picks/PickValueSummaryRow.tsx, frontend/src/components/picks/LeaguePickList.tsx, frontend/src/components/EvaluationOutputPanel.tsx</files>
  <action>
**AssetChip.tsx (MODIFY):** Extend the pick chip variant only per UI-SPEC "PickValueDisplay" section.
- When type === 'pick' AND pick value data is available: change layout from `inline-flex items-center gap-2` to `flex flex-col gap-1`
  - First row: existing [pick identity] [slot badge] + NEW [TimingBadge]
  - Second row: timing reasoning (text-xs text-muted-foreground, mt-1)
- When type === 'pick' but no pick value data: render existing layout unchanged (backward compatible)
- When type !== 'pick' (player chips): UNCHANGED — single-row inline-flex. Verify this explicitly per UI-SPEC note 3.
- Accept optional `pickValue?: PickValue` prop on the chip (or via context/lookup). Pass timing_label and timing_reasoning to TimingBadge.
- For future-year picks (year 2+ out): append "(future year)" in text-muted-foreground after pick identity per Copywriting Contract. Omit slot range badge per UI-SPEC note 6.

**PickValueSummaryRow.tsx (NEW):** For trade evaluator context per UI-SPEC "PickValueSummaryRow" section.
- Props: `pickValue: PickValue`, `managerName?: string`
- Row 1: pick identity + slot badge + TimingBadge (flex items-center gap-2)
- Row 2: "League value:" + league_adjusted_value (text-foreground) + "To {managerName}:" + demand_adjusted_value (text-primary) — flex items-center gap-3 mt-1
  - If no managerName: "To counterparty:" per Copywriting Contract
  - If no manager profiles exist (demand_adjusted equals league_adjusted): omit the demand row entirely per UI-SPEC note 9
- Row 3: timing reasoning (text-xs text-muted-foreground, mt-1)

**LeaguePickList.tsx (NEW):** Per UI-SPEC "LeaguePickList" section.
- Props: `leagueId: string`
- Uses `useLeaguePickValues(leagueId)` hook
- Header row: "Pick Capital" heading (text-lg font-semibold) left, "Last computed: {relative time}" + "Recompute Picks" button (Button variant="ghost" size="sm") right — flex items-center justify-between per UI-SPEC note 8
- "Last computed" uses most recent computed_at from batch per UI-SPEC note 5
- Per-pick rows per UI-SPEC spec: [Year+Round] [Owner] [Slot range] [TimingBadge] [Value right-aligned] + reasoning second row
- Loading state: Skeleton rows (h-11 w-full) x N per UI-SPEC
- Empty state: "No future picks tracked for this league." per Copywriting Contract
- Error state: "Pick values unavailable. Try recomputing or check the backend." per Copywriting Contract
- "Recompute Picks" button calls useRecomputePicks mutation

**EvaluationOutputPanel.tsx (MODIFY):** Where picks appear in trade evaluator party columns, replace the simple pick chip with PickValueSummaryRow for richer display. Use the expanded layout only in this trade evaluator context per UI-SPEC note.
  </action>
  <verify>
    <automated>cd /Users/jakyeamos/Desktop/Fantasy/frontend && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>AssetChip pick variant shows timing badge and reasoning, PickValueSummaryRow shows dual values in trade context, LeaguePickList renders batch pick values with recompute button, all TypeScript compiles and builds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Phase 6 pick valuation end-to-end</name>
  <action>
Verify the complete Phase 6 dynamic pick valuation integration across all display surfaces.

Start the backend: `cd backend && python -m uvicorn fantasy.main:app --reload`
Start the frontend: `cd frontend && npm run dev`

Test 1 — League pick list (PICK-01):
- Navigate to any league drill-in page
- Verify the Pick Capital section shows dynamic values per pick with timing badges
- Each pick should show: year+round, owner, slot range (~1.XX), colored timing badge, value number
- Timing reasoning appears below each pick row
- "Last computed" timestamp and "Recompute Picks" button visible in section header
- Click "Recompute Picks" — values should refresh

Test 2 — Trade evaluator picks (PICK-02 + D-04):
- Navigate to the trade evaluator
- Add a pick to a trade
- Verify the pick shows: timing badge, league-adjusted value, and demand-adjusted value (labeled "To {manager}:")
- If manager profiles exist: demand-adjusted value should differ from league-adjusted
- If no profiles: only league-adjusted shown (no "To counterparty" with identical value)

Test 3 — Timing recommendations (PICK-03 + D-05/D-06/D-07):
- Verify timing badges use correct colors: amber for "Sell now", blue for "Hold", green for "On the clock"
- Verify each timing badge has a one-line reasoning string below it
- Verify badges appear inline with pick data (not in a separate section) per D-07

Test 4 — Future-year picks:
- If any future-year picks (2+ years out) exist: verify they show "(future year)" annotation and no slot range badge
- Verify their values are lower than equivalent current-year picks (future discount applied)

Test 5 — Phase 5 unchanged:
- Verify the trade evaluator still works for player assets (not picks) — no regressions
- Verify player AssetChips are unchanged (single-row, no timing badge)
  </action>
  <verify>Human visual and interaction verification across 5 test areas</verify>
  <done>All 5 test areas pass: league pick list shows dynamic values, trade evaluator shows demand-adjusted values, timing badges have correct colors and reasoning, future-year picks annotated, Phase 5 player chips unchanged</done>
</task>

</tasks>

<verification>
- Frontend builds with no TypeScript errors
- TimingBadge renders all 3 states with correct Tailwind color classes
- AssetChip player variant is unchanged (no regression)
- LeaguePickList fetches batch pick values and renders all rows
- "Recompute Picks" triggers POST and refreshes the list
- Trade evaluator pick rows show demand-adjusted values
- All timing reasoning strings display per Copywriting Contract
</verification>

<success_criteria>
Dynamic pick values and timing recommendations are visible on all pick display surfaces. The upgrade is in-place (no new pages per D-03). Phase 5 trade evaluator picks now show dynamic values and timing context. Human verification confirms visual correctness and interaction flow.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dynamic-pick-valuation/06-04-SUMMARY.md`
</output>
