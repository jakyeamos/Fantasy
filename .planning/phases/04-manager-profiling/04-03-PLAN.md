---
phase: 04-manager-profiling
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - frontend/src/routes/league.$leagueId.managers.tsx
  - frontend/src/components/ManagerListRow.tsx
  - frontend/src/components/ExploitabilityScore.tsx
  - frontend/src/routes/league.$leagueId.tsx
autonomous: true
requirements: [MGR-01, MGR-02, MGR-03]

must_haves:
  truths:
    - "User can navigate to /league/:leagueId/managers and see a list of all managers with name, direction badge, exploitability score with evidence count, and top pitch angle preview"
    - "Exploitability score is always co-located with evidence count in the format 'Exploitability: {score} | {N} trades' per D-17"
    - "LOW CONFIDENCE managers show dimmed score (text-muted-foreground opacity-75) per D-16"
    - "Phase 3 ExploitWindowPanel rows link to individual dossiers and a 'View all managers' link appears at the bottom"
  artifacts:
    - path: "frontend/src/routes/league.$leagueId.managers.tsx"
      provides: "Managers list page route"
      contains: "createFileRoute"
    - path: "frontend/src/components/ManagerListRow.tsx"
      provides: "Individual manager row card"
      contains: "ManagerListRow"
    - path: "frontend/src/components/ExploitabilityScore.tsx"
      provides: "Reusable score display component"
      contains: "ExploitabilityScore"
  key_links:
    - from: "frontend/src/routes/league.$leagueId.managers.tsx"
      to: "/profiling/leagues/$leagueId/managers"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*profiling.*managers"
    - from: "frontend/src/routes/league.$leagueId.tsx"
      to: "frontend/src/routes/league.$leagueId.managers.tsx"
      via: "Link to /league/$leagueId/managers"
      pattern: "View all managers"
---

<objective>
Build the managers list page, manager list row cards, and update the Phase 3 exploit window to link to manager dossiers. Install new shadcn components (tabs, table, alert) needed by Phase 4.

Purpose: Provides the entry points to manager profiling — the quick-scan list view (per D-02, D-03) and the navigation bridge from Phase 3 exploit windows.
Output: Working /league/:leagueId/managers route with data fetched from the profiling API.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-manager-profiling/04-CONTEXT.md
@.planning/phases/04-manager-profiling/04-RESEARCH.md
@.planning/phases/04-manager-profiling/04-UI-SPEC.md
@.planning/phases/04-manager-profiling/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and create managers list page with ManagerListRow</name>
  <files>
    frontend/src/routes/league.$leagueId.managers.tsx,
    frontend/src/components/ManagerListRow.tsx,
    frontend/src/components/ExploitabilityScore.tsx
  </files>
  <read_first>
    frontend/src/routes/league.$leagueId.tsx,
    frontend/src/routes/index.tsx,
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/skeleton.tsx,
    frontend/package.json
  </read_first>
  <action>
    First, install shadcn components needed for Phase 4:
    ```bash
    cd frontend && npx shadcn@latest add tabs table alert --yes
    ```

    Create `frontend/src/components/ExploitabilityScore.tsx` — reusable score display (used in list row AND dossier header):
    ```tsx
    // Props: score: number, evidenceCount: number, lowConfidence: boolean
    // Renders: "Exploitability: {score} | {N} trades" per D-17
    // When lowConfidence=true: score number uses "text-muted-foreground opacity-75 font-semibold"
    // When lowConfidence=false: score number uses "text-primary font-semibold"
    // Evidence count always "text-muted-foreground"
    // Outer span: "text-xs text-muted-foreground" (Label, 12px)
    ```
    Exact markup per RESEARCH.md Example 4 and UI-SPEC ManagerListRow spec.

    Create `frontend/src/components/ManagerListRow.tsx`:
    - Props: `manager: ManagerSummary` (matching the API response type)
    - Define TypeScript type `ManagerSummary` inline or in a types file:
      ```typescript
      type PitchAngle = {
        rank: number; deal_archetype: string; send_description: string;
        avoid_description: string; reasoning: string;
      }
      type ManagerSummary = {
        league_id: string; roster_id: number; manager_name: string;
        direction_label: string | null; exploitability_score: number;
        evidence_count: number; low_confidence: boolean;
        top_pitch_angle: PitchAngle | null;
      }
      ```
    - Layout per UI-SPEC ManagerListRow spec (within a Card):
      1. Top row: `flex items-center gap-2 justify-between`
         - Manager name: `text-sm font-semibold` (Body 14px/600)
         - Direction label: `Badge variant="secondary"` with `text-xs` (Label 12px)
         - ExploitabilityScore component
      2. Preview row (`mt-1`):
         - If `top_pitch_angle` exists: deal archetype label (`text-xs text-primary`) + reasoning (`text-xs text-muted-foreground` truncated at 80 chars with ellipsis via `truncate max-w-[300px]`)
         - If null: "Insufficient trade history for pitch angle." per UI-SPEC copywriting
      3. Card wrapper: entire card is a `Link` (TanStack Router) to `/league/${manager.league_id}/managers/${manager.roster_id}`
      4. Card states: `hover:shadow-md transition-shadow`, min height 60px
    - No exploitation type on the list row per D-02

    Create `frontend/src/routes/league.$leagueId.managers.tsx`:
    - Use `createFileRoute` from TanStack Router (match existing route pattern)
    - Use `useQuery` from TanStack Query to fetch `/profiling/leagues/${leagueId}/managers`
    - Define query key: `["managers", leagueId]`
    - Page layout per UI-SPEC ManagersListPage:
      1. Page heading: "Managers" (Heading 18px/600) + league name in `text-muted-foreground` (Body 14px), `flex items-baseline gap-2`
      2. Manager list: `flex flex-col gap-3`
      3. Map over results rendering `ManagerListRow` for each
    - Loading state: 3 Skeleton cards matching row heights (`h-[60px] w-full` per card)
    - Empty state: "No managers found for this league." (Body 14px, centered, `text-muted-foreground`)
    - Error state: "Manager data unavailable. Check that the backend is running, then refresh." per UI-SPEC copywriting
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && echo "---" && test -f src/routes/league.\$leagueId.managers.tsx && echo "route exists" && test -f src/components/ManagerListRow.tsx && echo "row exists" && test -f src/components/ExploitabilityScore.tsx && echo "score exists" && test -f src/components/ui/tabs.tsx && echo "tabs installed" && test -f src/components/ui/table.tsx && echo "table installed" && test -f src/components/ui/alert.tsx && echo "alert installed"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ui/tabs.tsx exists (shadcn tabs installed)
    - frontend/src/components/ui/table.tsx exists (shadcn table installed)
    - frontend/src/components/ui/alert.tsx exists (shadcn alert installed)
    - ExploitabilityScore.tsx contains `text-primary font-semibold` (normal score state)
    - ExploitabilityScore.tsx contains `text-muted-foreground opacity-75` (low confidence state)
    - ExploitabilityScore.tsx contains the exact format pattern `Exploitability:` and `trades`
    - ManagerListRow.tsx contains `Badge variant="secondary"` (direction label)
    - ManagerListRow.tsx contains `hover:shadow-md transition-shadow`
    - ManagerListRow.tsx contains `Link` import from TanStack Router
    - ManagerListRow.tsx contains `Insufficient trade history for pitch angle` (empty pitch angle copy)
    - ManagerListRow.tsx does NOT contain exploitation type display (per D-02)
    - league.$leagueId.managers.tsx contains `createFileRoute`
    - league.$leagueId.managers.tsx contains `useQuery`
    - league.$leagueId.managers.tsx contains `/profiling/leagues/`
    - league.$leagueId.managers.tsx contains `No managers found for this league`
    - league.$leagueId.managers.tsx contains `Skeleton`
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>Managers list page renders all managers with name, direction badge, co-located exploitability score, and pitch angle preview; all four interaction states (loading, success, empty, error) handled.</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 3 ExploitWindowPanel with dossier links</name>
  <files>frontend/src/routes/league.$leagueId.tsx</files>
  <read_first>
    frontend/src/routes/league.$leagueId.tsx,
    frontend/src/routes/league.$leagueId.managers.tsx
  </read_first>
  <action>
    Modify `frontend/src/routes/league.$leagueId.tsx` to add dossier navigation per D-01 and UI-SPEC Routing Structure:

    1. Find the `ExploitWindowPanel` or exploit window section (Phase 3 `<details>/<summary>` rows for leaguemates showing behavioral triggers per DASH-04).

    2. Inside each exploit window row's expanded `<details>` content area, add a `Link` (TanStack Router) with text "View Dossier" pointing to `/league/${leagueId}/managers/${managerId}`:
       - Style: `text-primary text-sm font-semibold hover:underline`
       - The link is INSIDE the expanded content, not on the summary element itself
       - The `<summary>` click behavior (expand/collapse) MUST be preserved — navigating happens only via the "View Dossier" link click per UI-SPEC implementation note #6

    3. At the bottom of the entire ExploitWindowPanel (after all rows), add a "View all managers" link:
       - `Link` to `/league/${leagueId}/managers`
       - Style: `text-primary text-sm hover:underline mt-2 block`
       - Text: "View all managers" (exact per UI-SPEC copywriting)

    4. Import `Link` from TanStack Router if not already imported.

    IMPORTANT: Do not break existing Phase 3 expand/collapse behavior. The `<details>/<summary>` interaction must still work. Only add link elements inside the expanded content area and at the panel bottom.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10 && grep -c "View Dossier" src/routes/league.\$leagueId.tsx && grep -c "View all managers" src/routes/league.\$leagueId.tsx</automated>
  </verify>
  <acceptance_criteria>
    - league.$leagueId.tsx contains `View Dossier` string
    - league.$leagueId.tsx contains `View all managers` string
    - league.$leagueId.tsx contains Link import from TanStack Router
    - league.$leagueId.tsx contains `/managers/` in a Link href (dossier link path)
    - league.$leagueId.tsx contains `/managers` in a Link href (all managers link path)
    - league.$leagueId.tsx still contains `<details` or equivalent expand/collapse pattern (Phase 3 behavior preserved)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Phase 3 exploit window rows link to individual dossiers; "View all managers" link appears at bottom of panel; expand/collapse behavior preserved.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `grep "View Dossier" frontend/src/routes/league.$leagueId.tsx` matches
- `grep "View all managers" frontend/src/routes/league.$leagueId.tsx` matches
- Managers list route file exists and uses TanStack Query for data fetching
- ExploitabilityScore component enforces co-located format per D-17
</verification>

<success_criteria>
- shadcn tabs, table, alert components installed
- Managers list page fetches from /profiling API and renders ManagerListRow cards
- Each row shows name + direction badge + exploitability score with evidence count + top pitch angle preview
- LOW CONFIDENCE scores are visually dimmed per D-16
- Phase 3 ExploitWindowPanel has "View Dossier" links and "View all managers" link
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-manager-profiling/04-03-SUMMARY.md`
</output>
