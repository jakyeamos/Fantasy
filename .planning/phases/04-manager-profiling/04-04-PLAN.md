---
phase: 04-manager-profiling
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - frontend/src/routes/league.$leagueId.managers.$managerId.tsx
  - frontend/src/components/DossierPage.tsx
  - frontend/src/components/DossierOverviewTab.tsx
  - frontend/src/components/DossierTradeHistoryTab.tsx
  - frontend/src/components/DossierPitchAnglesTab.tsx
autonomous: false

requirements: [MGR-01, MGR-02, MGR-03, MGR-04]

must_haves:
  truths:
    - "User can view a dossier per leaguemate with tabbed layout: Overview | Trade History | Pitch Angles"
    - "Overview tab shows roster summary, direction, positional needs, exploitation types with evidence, aggregate trade stats, and LOW CONFIDENCE banner when applicable"
    - "Trade History tab shows all trades grouped by recency with value delta per trade (green positive, red negative)"
    - "Pitch Angles tab shows 2-3 pre-computed pitch angles with archetype label, send/avoid details, and reasoning"
    - "LOW CONFIDENCE banner is amber (not red) with AlertTriangle icon and exact copy from UI-SPEC"
    - "Exploitation type badges show primary (default variant) and secondary (secondary variant) per D-09"
  artifacts:
    - path: "frontend/src/routes/league.$leagueId.managers.$managerId.tsx"
      provides: "Dossier page route"
      contains: "createFileRoute"
    - path: "frontend/src/components/DossierPage.tsx"
      provides: "Tabs container with header"
      contains: "Tabs"
    - path: "frontend/src/components/DossierOverviewTab.tsx"
      provides: "Overview tab content"
      contains: "LOW CONFIDENCE"
    - path: "frontend/src/components/DossierTradeHistoryTab.tsx"
      provides: "Trade history table"
      contains: "TableRow"
    - path: "frontend/src/components/DossierPitchAnglesTab.tsx"
      provides: "Pitch angle cards"
      contains: "deal_archetype"
  key_links:
    - from: "frontend/src/routes/league.$leagueId.managers.$managerId.tsx"
      to: "/profiling/leagues/$leagueId/managers/$rosterId"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*profiling.*managers"
    - from: "frontend/src/components/DossierOverviewTab.tsx"
      to: "Alert component"
      via: "amber LOW CONFIDENCE banner"
      pattern: "bg-amber-50"
---

<objective>
Build the manager dossier page with three tabbed views (Overview, Trade History, Pitch Angles) and a human-verify checkpoint for the full manager profiling flow.

Purpose: This is the primary intelligence surface of Phase 4 — where the user studies a specific manager's behavioral patterns and decides how to exploit them in trade negotiations.
Output: Working /league/:leagueId/managers/:managerId dossier with all three tabs and complete interaction states.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-manager-profiling/04-CONTEXT.md
@.planning/phases/04-manager-profiling/04-RESEARCH.md
@.planning/phases/04-manager-profiling/04-UI-SPEC.md
@.planning/phases/04-manager-profiling/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DossierPage with tabs and all three tab components</name>
  <files>
    frontend/src/routes/league.$leagueId.managers.$managerId.tsx,
    frontend/src/components/DossierPage.tsx,
    frontend/src/components/DossierOverviewTab.tsx,
    frontend/src/components/DossierTradeHistoryTab.tsx,
    frontend/src/components/DossierPitchAnglesTab.tsx
  </files>
  <read_first>
    frontend/src/routes/league.$leagueId.managers.tsx,
    frontend/src/components/ExploitabilityScore.tsx,
    frontend/src/components/ui/tabs.tsx,
    frontend/src/components/ui/table.tsx,
    frontend/src/components/ui/alert.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/separator.tsx,
    frontend/src/components/ui/skeleton.tsx
  </read_first>
  <action>
    Define TypeScript type `ManagerProfile` (matching API response) in a shared types location or inline:
    ```typescript
    type ManagerProfile = {
      league_id: string; roster_id: number; computed_at: string;
      evidence_count: number; low_confidence: boolean;
      exploitability_score: number;
      exploitation_primary: string | null;
      exploitation_secondary: string | null;
      exploitation_evidence: Record<string, string>;
      pitch_angles: PitchAngle[];
      aggregate_trade_stats: { total_trades: number; win_rate: number; avg_delta: number };
      roster_summary: { direction_label: string; positional_needs: string[] } | null;
    }
    ```

    Create `frontend/src/routes/league.$leagueId.managers.$managerId.tsx`:
    - Use `createFileRoute` matching existing pattern
    - Use `useQuery` to fetch `/profiling/leagues/${leagueId}/managers/${rosterId}`
    - Query key: `["manager-dossier", leagueId, managerId]`
    - Render `DossierPage` component with fetched data
    - Loading state: Skeleton components per UI-SPEC skeleton dimensions (h-7 w-48 for name, h-5 w-64 for badges, h-4 w-40 for score, h-24 w-full for tab content)
    - Error state: "Dossier unavailable. Try refreshing or check the backend connection." per UI-SPEC

    Create `frontend/src/components/DossierPage.tsx`:
    - Props: `profile: ManagerProfile`
    - Layout per UI-SPEC DossierPage spec:
      1. Dossier header (fixed above tabs):
         - Manager name: `text-xl font-semibold` (Display 20px/600) — derive from roster data or pass as prop
         - Direction badge: `Badge variant="secondary"` showing `profile.roster_summary?.direction_label`
         - ExploitabilityScore component (reuse from Plan 03) with `score`, `evidenceCount`, `lowConfidence` props
      2. `Tabs defaultValue="overview"`:
         - `TabsList` with 3 `TabsTrigger` elements: "Overview", "Trade History", "Pitch Angles" (exact casing per UI-SPEC)
         - TabsTrigger min height 44px (`h-11`)
         - `TabsContent value="overview"` → DossierOverviewTab
         - `TabsContent value="trade-history"` → DossierTradeHistoryTab
         - `TabsContent value="pitch-angles"` → DossierPitchAnglesTab
      3. Tab state is local (not URL param) per UI-SPEC "Tab persistence" section

    Create `frontend/src/components/DossierOverviewTab.tsx`:
    - Props: `profile: ManagerProfile`
    - Layout per UI-SPEC DossierOverviewTab spec (top to bottom, Separator my-4 between sections):

      1. **LOW CONFIDENCE Alert** (conditional: shown only when `profile.low_confidence === true`):
         ```tsx
         <Alert className="bg-amber-50 dark:bg-amber-950/20 border-amber-200 dark:border-amber-800">
           <AlertTriangle className="h-4 w-4 text-amber-600" />
           <AlertTitle className="text-amber-800 dark:text-amber-300 text-xs font-semibold">
             LOW CONFIDENCE
           </AlertTitle>
           <AlertDescription className="text-amber-700 dark:text-amber-400">
             Based on {profile.evidence_count} trades (minimum 10 for reliable profiling).
             Treat all conclusions with skepticism.
           </AlertDescription>
         </Alert>
         ```
         Per D-16: amber, not destructive red. Import AlertTriangle from lucide-react.

      2. **Exploitation Type section**:
         - Section heading: "Exploitation Type" (`text-lg font-semibold mb-2`)
         - Primary type: `Badge variant="default"` with exploitation type label (exact lowercase: "value-loss trader", etc. per D-10)
         - Secondary type (if not null): `Badge variant="secondary"` with `ml-2`
         - Evidence summary below each type: `text-sm text-muted-foreground` — render `profile.exploitation_evidence[type_key]` values (e.g., "Lost value on 7 of 10 trades" per D-11)

      3. **Roster Summary section**:
         - Section heading: "Roster Summary" (`text-lg font-semibold`)
         - Direction label + reasoning: `text-sm`
         - Positional needs: `<ul>` with `<li>` items, `text-sm`, max 4 items

      4. **Aggregate Trade Stats row**:
         - `flex gap-6` horizontal layout of stat cells:
           - Total Trades: number (`text-xl font-semibold`) + label below (`text-xs text-muted-foreground`)
           - Win Rate: formatted as percentage + label
           - Avg Value Delta: formatted with +/- sign, green/red per UI-SPEC color section + label

    Create `frontend/src/components/DossierTradeHistoryTab.tsx`:
    - Props: `profile: ManagerProfile` (trade history comes from profile or a separate query)
    - Use shadcn Table (TableHeader, TableBody, TableRow, TableHead, TableCell)
    - Columns per UI-SPEC: Date | Assets Sent | Assets Received | Value Delta
    - Date column: `text-xs` formatted as "MMM YYYY" (e.g., "Jan 2025")
    - Assets columns: `text-sm`, comma-separated, truncate at 40 chars per cell
    - Value Delta: `text-xs`, `text-green-600` for positive (+X.XX), `text-red-600` for negative (-X.XX)
    - Rows grouped: "Last 12 Months" and "Older" — group headings `text-xs font-semibold text-muted-foreground bg-muted/50 px-4 py-1`
    - Default sort: newest first within each group
    - TableRow min height 44px: `h-11` class
    - Empty state: "No trade history found for this manager." centered, `text-sm text-muted-foreground`
    - Error state: "Trade history unavailable. Try refreshing." centered, `text-sm`
    - Loading: 5 skeleton rows `h-11 w-full`

    Create `frontend/src/components/DossierPitchAnglesTab.tsx`:
    - Props: `pitchAngles: PitchAngle[]`
    - Layout per UI-SPEC: `flex flex-col gap-4`, one Card per angle
    - Each pitch angle card (CardContent):
      1. Deal archetype label: `text-lg font-semibold text-primary` (e.g., "Win-now swap")
      2. Two detail rows:
         - "Send:" label (`text-xs text-muted-foreground`) + value (`text-sm`) side by side
         - "Avoid:" label (`text-xs text-muted-foreground`) + value (`text-sm`) side by side
      3. Reasoning: `text-sm text-muted-foreground italic mt-2 pt-2 border-t` — full sentence, no truncation
    - Empty state: "No pitch angles available yet. More trade history is needed to compute angles." centered, `text-sm text-muted-foreground`
    - Error state: "Pitch angles unavailable. Try refreshing." centered, `text-sm`
    - Loading: 2 skeleton cards `h-24 w-full`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && echo "---" && test -f src/routes/league.\$leagueId.managers.\$managerId.tsx && echo "dossier route exists" && test -f src/components/DossierPage.tsx && echo "DossierPage exists" && test -f src/components/DossierOverviewTab.tsx && echo "overview exists" && test -f src/components/DossierTradeHistoryTab.tsx && echo "trade history exists" && test -f src/components/DossierPitchAnglesTab.tsx && echo "pitch angles exists"</automated>
  </verify>
  <acceptance_criteria>
    - league.$leagueId.managers.$managerId.tsx contains `createFileRoute`
    - league.$leagueId.managers.$managerId.tsx contains `useQuery`
    - league.$leagueId.managers.$managerId.tsx contains `/profiling/leagues/`
    - league.$leagueId.managers.$managerId.tsx contains `Dossier unavailable`
    - DossierPage.tsx contains `Tabs` import
    - DossierPage.tsx contains `TabsTrigger` with values "Overview", "Trade History", "Pitch Angles"
    - DossierPage.tsx contains `defaultValue="overview"`
    - DossierOverviewTab.tsx contains `bg-amber-50 dark:bg-amber-950/20` (amber banner)
    - DossierOverviewTab.tsx contains `border-amber-200 dark:border-amber-800` (amber border)
    - DossierOverviewTab.tsx contains `AlertTriangle` import from lucide-react
    - DossierOverviewTab.tsx contains `LOW CONFIDENCE` string
    - DossierOverviewTab.tsx contains `Based on` and `trades (minimum 10 for reliable profiling)` (banner body)
    - DossierOverviewTab.tsx contains `Badge variant="default"` (primary exploitation type)
    - DossierOverviewTab.tsx contains `Badge variant="secondary"` (secondary exploitation type)
    - DossierOverviewTab.tsx contains `Exploitation Type` heading
    - DossierOverviewTab.tsx contains `Roster Summary` heading
    - DossierTradeHistoryTab.tsx contains `TableRow` or `TableBody` (shadcn table)
    - DossierTradeHistoryTab.tsx contains `text-green-600` (positive delta)
    - DossierTradeHistoryTab.tsx contains `text-red-600` (negative delta)
    - DossierTradeHistoryTab.tsx contains `Last 12 Months` group heading
    - DossierTradeHistoryTab.tsx contains `No trade history found for this manager`
    - DossierPitchAnglesTab.tsx contains `text-primary` (archetype label accent)
    - DossierPitchAnglesTab.tsx contains `Send:` and `Avoid:` labels
    - DossierPitchAnglesTab.tsx contains `italic` (reasoning style)
    - DossierPitchAnglesTab.tsx contains `No pitch angles available yet`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dossier page renders with all three tabs; Overview shows exploitation types with evidence and LOW CONFIDENCE banner; Trade History shows value deltas with color coding; Pitch Angles shows archetype cards with send/avoid/reasoning.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full manager profiling flow</name>
  <files>none</files>
  <action>
    Human verifies the complete manager profiling system built across Plans 01-04.

    Steps:
    1. Start backend: `cd backend && uvicorn fantasy.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Ensure at least one league is ingested with trade history (run ingest if needed)
    4. Trigger profile computation: `curl -X POST http://localhost:8000/profiling/leagues/{LEAGUE_ID}/managers/compute` — confirm response `{"computed": N}` where N > 0
    5. Visit `http://localhost:5173/league/{LEAGUE_ID}/managers` — verify:
       - Page heading "Managers" is visible
       - Each manager row shows: name, direction badge, exploitability score in "Exploitability: XX | N trades" format, pitch angle preview
       - LOW CONFIDENCE managers (fewer than 10 trades) show dimmed score
    6. Click a manager row to open dossier — verify:
       - Header shows manager name, direction badge, exploitability score
       - "Overview" tab is active by default
       - Exploitation type badges show primary (colored) and secondary (muted) if applicable
       - Evidence strings show aggregate format (e.g., "Lost value on 7 of 10 trades")
       - Aggregate trade stats show Total Trades, Win Rate, Avg Value Delta
       - If LOW CONFIDENCE: amber banner at top with AlertTriangle icon and exact copy
    7. Click "Trade History" tab — verify:
       - Table shows Date, Assets Sent, Assets Received, Value Delta columns
       - Positive deltas are green, negative deltas are red
       - Grouped by "Last 12 Months" and "Older"
    8. Click "Pitch Angles" tab — verify:
       - 2-3 cards with archetype label (accent color), Send/Avoid lines, reasoning in italic
    9. Navigate back to league drill-in page — verify:
       - Exploit window rows have "View Dossier" link inside expanded content
       - "View all managers" link at bottom of exploit window panel
  </action>
  <verify>Human approves full flow or describes issues</verify>
  <done>User confirms managers list, dossier tabs, exploit window links all function correctly. Type "approved" or describe issues found.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- All 5 new component/route files exist
- Dossier page uses shadcn Tabs with 3 tab triggers
- LOW CONFIDENCE banner uses amber styling (not destructive red)
- Exploitation type labels are exact lowercase strings per D-10
- Trade history uses shadcn Table with value delta coloring
- Human verification confirms end-to-end flow works
</verification>

<success_criteria>
- User can navigate to /league/:leagueId/managers/:managerId and see a complete dossier
- Overview tab shows exploitation types, evidence, roster summary, trade stats, and conditional LOW CONFIDENCE banner
- Trade History tab shows grouped trades with value delta coloring
- Pitch Angles tab shows 2-3 pre-computed angles with archetype, send/avoid, reasoning
- All interaction states (loading, success, empty, error) handled in every component
- Human checkpoint approves the full Phase 4 flow
</success_criteria>

<output>
After completion, create `.planning/phases/04-manager-profiling/04-04-SUMMARY.md`
</output>
