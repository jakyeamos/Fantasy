---
phase: 03-core-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - frontend/src/routes/index.tsx
  - frontend/src/routes/league.$leagueId.tsx
  - frontend/src/components/LeagueCard.tsx
  - frontend/src/components/RisersFallersList.tsx
  - frontend/src/components/ExploitWindowPanel.tsx
  - frontend/src/components/SnapshotStatus.tsx
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, PORT-01]

must_haves:
  truths:
    - "User sees a card per connected league on load showing direction label, confidence band, and primary weakness -- visible without scrolling or expanding (DASH-01, D-03)"
    - "Clicking a league card navigates to /league/$leagueId route (D-04)"
    - "League card shows the single top exploit window per league (D-10)"
    - "League card shows last snapshot timestamp (D-18)"
    - "Drill-in view shows top 5 risers and top 5 fallers with player name, delta, and reason (D-06, D-07)"
    - "Drill-in view shows exploit windows grouped by manager with expand/collapse (D-12)"
    - "Each trigger includes a suggested action string (D-13)"
    - "Multiple triggers per manager collapse into High Opportunity signal (D-14)"
    - "Snapshot now button exists on both dashboard and drill-in views (D-17)"
    - "Cross-league exposure alert shows players owned in 2+ leagues (DASH-03)"
    - "Loading states use Skeleton components matching content dimensions"
    - "Empty and error states render exact copy from UI-SPEC"
  artifacts:
    - path: "frontend/src/components/LeagueCard.tsx"
      provides: "League card component with direction label, confidence badge, weakness, exploit window"
      contains: "LeagueCard"
    - path: "frontend/src/components/RisersFallersList.tsx"
      provides: "Top 5 risers and fallers table"
      contains: "RisersFallersList"
    - path: "frontend/src/components/ExploitWindowPanel.tsx"
      provides: "Exploit windows grouped by manager with expand/collapse"
      contains: "ExploitWindowPanel"
    - path: "frontend/src/components/SnapshotStatus.tsx"
      provides: "Snapshot timestamp display and manual trigger button"
      contains: "SnapshotStatus"
  key_links:
    - from: "frontend/src/routes/index.tsx"
      to: "frontend/src/api/queries.ts"
      via: "useQuery(dashboardSummaryOptions)"
      pattern: "useQuery.*dashboardSummaryOptions"
    - from: "frontend/src/routes/league.$leagueId.tsx"
      to: "frontend/src/api/queries.ts"
      via: "useQuery(leagueDetailOptions(leagueId))"
      pattern: "useQuery.*leagueDetailOptions"
    - from: "frontend/src/components/SnapshotStatus.tsx"
      to: "/api/snapshots/trigger"
      via: "useMutation with POST fetch"
      pattern: "useMutation"
---

> Execution boundary note (2026-03-21): See `.planning/phases/03-core-dashboard/03-ENGINEER-HANDOFF.md` for Codex-owned work, stop conditions, and deferred items.

<objective>
Build all dashboard UI components and wire them into the route shells: LeagueCard grid on the main dashboard, full drill-in view with risers/fallers, exploit windows, and snapshot controls.

Purpose: This is the user-facing delivery of Phase 3. After this plan, the user sees league cards with direction labels, value movers, exploit opportunities, and snapshot controls -- the core intelligence surface.

Output: Complete, functional dashboard with all components rendering data from the backend API, including loading, empty, and error states per the UI-SPEC.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-dashboard/03-CONTEXT.md
@.planning/phases/03-core-dashboard/03-RESEARCH.md
@.planning/phases/03-core-dashboard/03-UI-SPEC.md
@.planning/phases/03-core-dashboard/03-01-SUMMARY.md
@.planning/phases/03-core-dashboard/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 02: API types the components consume -->

From frontend/src/api/types.ts:
```typescript
export interface DashboardLeagueSummary {
  league_id: string
  league_name: string
  direction_label: string
  confidence_band: 'High' | 'Medium' | 'Low' | '--'
  primary_weakness: string
  top_exploit_window: string | null
  last_snapshot_at: string | null
  last_ingest_at: string | null
}

export interface RiserFallerEntry {
  player_name: string
  delta: number
  reason: string
}

export interface ExploitTrigger {
  type: string
  description: string
  suggested_action: string
}

export interface ExploitWindowManager {
  roster_id: number
  trigger_count: number
  is_high_opportunity: boolean
  triggers: ExploitTrigger[]
}

export interface LeagueDetailResponse {
  league_id: string
  league_name: string
  direction_label: string
  confidence_band: 'High' | 'Medium' | 'Low' | '--'
  primary_weakness: string
  risers: RiserFallerEntry[]
  fallers: RiserFallerEntry[]
  exploit_windows: ExploitWindowManager[]
  last_snapshot_at: string | null
  last_ingest_at: string | null
}

export interface SnapshotTriggerResponse {
  snapshot_ids: number[]
  count: number
}
```

From frontend/src/api/queries.ts:
```typescript
export const dashboardSummaryOptions = queryOptions({ queryKey: ['dashboard', 'summary'], ... })
export const leagueDetailOptions = (leagueId: string) => queryOptions({ queryKey: ['league', leagueId], ... })
export const snapshotStatusOptions = queryOptions({ queryKey: ['snapshots', 'status'], ... })
```

From frontend/src/components/ui/:
- card.tsx: Card, CardContent, CardHeader, CardTitle
- badge.tsx: Badge (variants: default, secondary, outline, destructive)
- button.tsx: Button (variants: default, secondary, outline, ghost, link; sizes: default, sm, lg, icon)
- separator.tsx: Separator
- skeleton.tsx: Skeleton
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LeagueCard + SnapshotStatus components + main dashboard route</name>
  <files>
    frontend/src/components/LeagueCard.tsx,
    frontend/src/components/SnapshotStatus.tsx,
    frontend/src/routes/index.tsx
  </files>
  <read_first>
    frontend/src/api/types.ts,
    frontend/src/api/queries.ts,
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/skeleton.tsx,
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/separator.tsx,
    frontend/src/lib/utils.ts,
    .planning/phases/03-core-dashboard/03-UI-SPEC.md,
    .planning/phases/03-core-dashboard/03-CONTEXT.md
  </read_first>
  <action>
1. Create `frontend/src/components/LeagueCard.tsx`:
   - Props interface `LeagueCardProps` with fields from `DashboardLeagueSummary`
   - Compose shadcn Card + Badge + Link (TanStack Router)
   - Layout (top to bottom, per UI-SPEC Component Inventory > LeagueCard):
     a. `CardHeader` with `pb-2`:
        - `CardTitle` with `text-sm font-medium text-muted-foreground` showing league name (Heading role, but using sm for the subtitle treatment per UI-SPEC: "league name (Heading, 18px/600, text-muted-foreground)")
        - Direction label row: `flex items-center gap-2`
          - Direction label text: `text-xl font-semibold` (Display 20px/600 per UI-SPEC)
          - Confidence Badge: variant="default" for "High", variant="secondary" for "Medium", variant="outline" for "Low", variant="outline" for "--"
     b. `Separator`
     c. `CardContent` with `space-y-2`:
        - Primary weakness paragraph: `text-sm` (Body 14px/400)
        - Top exploit window (conditional, only if non-null): `text-xs text-muted-foreground border-t pt-2` (Label 12px/400)
        - Snapshot timestamp: `text-xs text-muted-foreground` showing relative time ("Last snapshot: X minutes ago" for <24h, date for older) or "No snapshot yet"
   - Card wrapper: `<Link to="/league/$leagueId" params={{ leagueId }}>` wrapping entire card (D-04)
   - Card classes: `cursor-pointer hover:shadow-md transition-shadow h-full min-h-[180px]` (per UI-SPEC: 180px min height)
   - Export named `LeagueCard`

2. Create `frontend/src/components/SnapshotStatus.tsx`:
   - Props: `{ lastSnapshotAt: string | null }`
   - Uses `useMutation` from TanStack Query to POST to `/api/snapshots/trigger`
   - On success: invalidate `['snapshots', 'status']` and `['dashboard', 'summary']` query keys
   - Layout per UI-SPEC > SnapshotStatus:
     - `flex items-center gap-2 text-xs text-muted-foreground`
     - Timestamp text: relative time if <24h ("Last snapshot: {N} minutes ago" / "{N} hours ago"), absolute date if older, or "No snapshot yet" if null
     - Button (shadcn `Button` with variant="default" size="sm"): text "Snapshot now", disabled + "Saving..." when mutation is pending
   - Relative time helper function: compute from ISO string to human-readable relative time. Use simple math (Date.now() - Date.parse()), no external library:
     - <1 min: "just now"
     - <60 min: "{N} minutes ago"
     - <24 hours: "{N} hours ago"
     - >=24 hours: format as locale date string
   - Export named `SnapshotStatus`

3. Replace `frontend/src/routes/index.tsx` dashboard route:
   - Use `useQuery(dashboardSummaryOptions)` to fetch league data
   - Implement all 4 states per UI-SPEC > Interaction States:
     - **Loading:** Grid of 3 Skeleton cards (each `min-h-[180px]` with inner Skeleton elements: `h-7 w-40` for direction label, `h-4 w-full` for weakness, `h-3 w-28` for timestamp -- per UI-SPEC Implementation Note 5)
     - **Error:** "Dashboard data unavailable. Check that the backend is running, then refresh." (exact copy per UI-SPEC Copywriting Contract)
     - **Empty (no leagues):** "No leagues connected. Add a league ID to get started." (exact copy)
     - **Success:** Grid layout `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 lg:gap-6 p-6 lg:px-8` (per UI-SPEC Routing Structure)
   - Map league data to LeagueCard components
   - Add a SnapshotStatus component below the grid (per D-17: manual trigger on main dashboard) -- pass the earliest last_snapshot_at from all leagues, or null if none
   - DASH-03 cross-league exposure: scan the summary data for players appearing in 2+ leagues. If the summary endpoint doesn't provide this directly, add a note placeholder "Cross-league alerts will appear here" for now (the backend endpoint needs to be enhanced). Actually, the dashboard summary endpoint from Plan 01 should include this. If not available in the response type, add a simple detection: this is a frontend-only concern -- collect player lists from the summary data if available. For Phase 3, display a static informational area: "Cross-league exposure alerts" with a placeholder message if no data. The backend Plan 01 does not include a dedicated cross-league endpoint -- this can be a simple client-side aggregation if player data is in the response, or a placeholder section.
  </action>
  <verify>
    <automated>cd /Users/jakyeamos/Desktop/Fantasy/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/LeagueCard.tsx contains `export function LeagueCard`
    - frontend/src/components/LeagueCard.tsx contains `<Link to="/league/$leagueId"` (D-04 separate route navigation)
    - frontend/src/components/LeagueCard.tsx contains `text-xl font-semibold` for direction label (Display 20px/600)
    - frontend/src/components/LeagueCard.tsx contains `Badge` with variant logic for High/Medium/Low
    - frontend/src/components/LeagueCard.tsx contains `min-h-[180px]` (UI-SPEC card min height)
    - frontend/src/components/LeagueCard.tsx contains `hover:shadow-md transition-shadow`
    - frontend/src/components/LeagueCard.tsx contains `Separator` between header and content
    - frontend/src/components/SnapshotStatus.tsx contains `export function SnapshotStatus`
    - frontend/src/components/SnapshotStatus.tsx contains `useMutation` for POST trigger
    - frontend/src/components/SnapshotStatus.tsx contains `Snapshot now` button text and `Saving...` pending text
    - frontend/src/components/SnapshotStatus.tsx contains `invalidateQueries` for both `snapshots` and `dashboard` keys
    - frontend/src/routes/index.tsx contains `useQuery(dashboardSummaryOptions)`
    - frontend/src/routes/index.tsx contains `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
    - frontend/src/routes/index.tsx contains `Skeleton` loading state
    - frontend/src/routes/index.tsx contains exact string `Dashboard data unavailable. Check that the backend is running, then refresh.`
    - frontend/src/routes/index.tsx contains exact string `No leagues connected. Add a league ID to get started.`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Main dashboard renders league card grid with direction labels, confidence bands, weaknesses, top exploit windows, and snapshot timestamps. Loading/empty/error states match UI-SPEC copy exactly. SnapshotStatus provides manual trigger on dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: RisersFallersList + ExploitWindowPanel + drill-in route</name>
  <files>
    frontend/src/components/RisersFallersList.tsx,
    frontend/src/components/ExploitWindowPanel.tsx,
    frontend/src/routes/league.$leagueId.tsx
  </files>
  <read_first>
    frontend/src/api/types.ts,
    frontend/src/api/queries.ts,
    frontend/src/components/LeagueCard.tsx,
    frontend/src/components/SnapshotStatus.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/skeleton.tsx,
    frontend/src/lib/utils.ts,
    .planning/phases/03-core-dashboard/03-UI-SPEC.md,
    .planning/phases/03-core-dashboard/03-CONTEXT.md
  </read_first>
  <action>
1. Create `frontend/src/components/RisersFallersList.tsx`:
   - Props: `{ risers: RiserFallerEntry[], fallers: RiserFallerEntry[] }`
   - Layout per UI-SPEC > RisersFallersList:
     - Two side-by-side sections: "Risers" (left) and "Fallers" (right) using `grid grid-cols-1 md:grid-cols-2 gap-6`
     - Section heading: `text-lg font-semibold mb-3` (Heading 18px/600) -- "Value Risers" and "Value Fallers"
     - Each section: HTML `<table>` with Tailwind classes (per UI-SPEC: no shadcn Table)
     - Table class: `w-full text-xs` (Label 12px/400)
     - Each row: 3 cells:
       a. Player name: `font-normal` (Label 12px/400)
       b. Delta: `text-green-600` for risers, `text-red-600` for fallers. Format as "+X.X" for positive, "-X.X" for negative (D-07)
       c. Reason: `text-muted-foreground truncate max-w-[200px]` -- truncated at ~60 chars with ellipsis via CSS `truncate`
     - Show exactly 5 rows per section (D-06), or fewer if less data
   - Empty state: "No actionable movers since last ingest." (exact copy, Body 14px/400, centered, `text-muted-foreground`)
   - Export named `RisersFallersList`

2. Create `frontend/src/components/ExploitWindowPanel.tsx`:
   - Props: `{ managers: ExploitWindowManager[] }`
   - Layout per UI-SPEC > ExploitWindowPanel:
     - Section heading: `text-lg font-semibold mb-3` -- "Exploit Windows"
     - Each manager: `<details>` / `<summary>` pattern styled with Tailwind (per UI-SPEC: no shadcn Accordion)
     - Summary row (collapsed state): `flex items-center justify-between min-h-[44px] cursor-pointer` (44px touch target per UI-SPEC)
       - Left: manager identifier `text-sm` (Body 14px/400) -- "Roster {roster_id}" (display roster_id for now; Phase 4 adds real manager names)
       - Right: trigger count `Badge variant="outline"` (Label 12px/400) showing "{count} trigger(s)"
       - If `is_high_opportunity` is true: add "High Opportunity" label with `text-sm font-semibold text-primary` (per UI-SPEC and D-14, exact casing "High Opportunity")
     - Expanded content: each trigger as a block:
       - Trigger description: `text-sm text-foreground` (Body 14px/400)
       - Suggested action: `text-sm text-muted-foreground italic` (Body 14px/400) -- per D-13, each trigger includes a suggested action
     - Style `<details>` with `border-b` between managers
   - Empty state: "No behavioral triggers detected for this league." (exact copy, Body 14px/400, centered, `text-muted-foreground`)
   - Export named `ExploitWindowPanel`

3. Replace `frontend/src/routes/league.$leagueId.tsx` drill-in route:
   - Use `useQuery(leagueDetailOptions(leagueId))` to fetch league detail
   - Get leagueId from `Route.useParams()`
   - Implement all 4 states:
     - **Loading:** Skeleton blocks for each section (risers table, fallers table, exploit panel)
     - **Error:** "League data unavailable. Try refreshing or check the backend connection." (exact copy)
     - **Success:** Full layout below
   - Layout structure (top to bottom):
     a. Back link: `<Link to="/">` with text "Back to Dashboard" (simple text link, `text-sm text-muted-foreground hover:text-foreground`)
     b. Header section: league name (Heading 18px/600), direction label + confidence badge (same rendering as LeagueCard), primary weakness text
     c. `Separator` with `my-6`
     d. RisersFallersList component (D-05, D-06, D-07, D-08)
     e. `Separator` with `my-6`
     f. ExploitWindowPanel component (D-11, D-12, D-13, D-14)
     g. `Separator` with `my-6`
     h. SnapshotStatus component (D-17: manual trigger available in drill-in view)
   - Container: `p-6 lg:px-8 max-w-5xl` (constrained width for readability in the detail view)
  </action>
  <verify>
    <automated>cd /Users/jakyeamos/Desktop/Fantasy/frontend && npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/RisersFallersList.tsx contains `export function RisersFallersList`
    - frontend/src/components/RisersFallersList.tsx contains `text-green-600` for risers and `text-red-600` for fallers
    - frontend/src/components/RisersFallersList.tsx contains `<table` (HTML table, not shadcn Table per UI-SPEC)
    - frontend/src/components/RisersFallersList.tsx contains exact string `No actionable movers since last ingest.`
    - frontend/src/components/ExploitWindowPanel.tsx contains `export function ExploitWindowPanel`
    - frontend/src/components/ExploitWindowPanel.tsx contains `<details` and `<summary` (native expand/collapse per UI-SPEC)
    - frontend/src/components/ExploitWindowPanel.tsx contains `min-h-[44px]` (touch target per UI-SPEC)
    - frontend/src/components/ExploitWindowPanel.tsx contains exact string `High Opportunity` (D-14 exact casing)
    - frontend/src/components/ExploitWindowPanel.tsx contains exact string `No behavioral triggers detected for this league.`
    - frontend/src/routes/league.$leagueId.tsx contains `useQuery(leagueDetailOptions(`
    - frontend/src/routes/league.$leagueId.tsx contains `Route.useParams()`
    - frontend/src/routes/league.$leagueId.tsx contains `RisersFallersList` and `ExploitWindowPanel` and `SnapshotStatus`
    - frontend/src/routes/league.$leagueId.tsx contains exact string `League data unavailable. Try refreshing or check the backend connection.`
    - frontend/src/routes/league.$leagueId.tsx contains `<Link to="/"` for back navigation
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Drill-in view renders risers/fallers table, exploit window panel with manager expand/collapse, and snapshot controls. All UI-SPEC copy, interaction states, and spacing requirements met. Full frontend builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete dashboard</name>
  <files>frontend/src/routes/index.tsx, frontend/src/routes/league.$leagueId.tsx</files>
  <action>
    Visual and functional verification of the complete Phase 3 dashboard. Start both backend and frontend dev servers, then verify all dashboard features in the browser per the how-to-verify checklist below.
  </action>
  <what-built>
    Complete Phase 3 dashboard: main view with league card grid (direction labels, confidence bands, weaknesses, exploit windows, snapshot timestamps) and drill-in view with risers/fallers, exploit window panel, and snapshot controls. Backend API endpoints serving dashboard data with graceful degradation for missing Phase 2 data.
  </what-built>
  <how-to-verify>
    1. Start the backend: `cd backend \&\& uvicorn fantasy.main:app --reload`
    2. Start the frontend: `cd frontend \&\& npm run dev`
    3. Open http://localhost:5173 in a browser
    4. **Main dashboard check:**
       - Verify league cards appear in a grid (or empty state if no leagues connected)
       - Verify each card shows direction label, confidence band (High/Medium/Low), and primary weakness without scrolling
       - Verify cards are clickable and navigate to /league/{id}
       - Verify "Snapshot now" button is visible and clickable
       - Verify last snapshot timestamp displays (or "No snapshot yet")
    5. **Drill-in check (click a league card):**
       - Verify risers/fallers tables show with player names, deltas (green/red), and reason strings
       - Verify exploit windows section shows with manager grouping and expand/collapse
       - Verify "Snapshot now" button is also present in drill-in
       - Verify "Back to Dashboard" link works
    6. **Error handling:** Stop the backend and refresh -- verify error message appears, not a blank screen
    7. **Loading states:** Refresh with backend running -- verify skeleton placeholders appear briefly before data loads
  </how-to-verify>
  <verify>Human visual inspection of running application per checklist above</verify>
  <done>User confirms dashboard renders correctly with all features visible and functional</done>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- `cd frontend && npx tsc --noEmit` passes
- All 4 custom components exist: LeagueCard.tsx, RisersFallersList.tsx, ExploitWindowPanel.tsx, SnapshotStatus.tsx
- Dashboard route uses dashboardSummaryOptions query
- Drill-in route uses leagueDetailOptions query
- All UI-SPEC copywriting strings are exact matches
- All interaction states (loading, success, empty, error) are implemented per UI-SPEC
</verification>

<success_criteria>
- User sees league cards on load with direction label, confidence, weakness visible without scrolling (DASH-01)
- Risers/fallers display per league with deltas and reasons (DASH-02)
- Cross-league exposure section exists on dashboard (DASH-03)
- Exploit windows show with suggested actions and manager grouping (DASH-04)
- Snapshot now button works on both dashboard and drill-in (PORT-01, D-17)
- Human verification confirms the full flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-dashboard/03-03-SUMMARY.md`
</output>
